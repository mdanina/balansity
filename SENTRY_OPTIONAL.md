# 📦 Sentry - Опциональная зависимость

## ⚠️ Важно

Sentry сделан **опциональным** - проект работает без него. Если вы хотите использовать мониторинг ошибок:

### Установка Sentry (опционально)

```bash
npm install @sentry/react
```

После установки Sentry автоматически начнет работать, если:
1. Пакет `@sentry/react` установлен
2. В `.env.production` указан `VITE_SENTRY_DSN`

### Без Sentry

Проект работает нормально без Sentry:
- ✅ Все функции работают
- ✅ Ошибки логируются в консоль (как раньше)
- ✅ ErrorBoundary работает
- ❌ Ошибки не отправляются в Sentry (но это не критично)

### Для production (рекомендуется)

1. Установите Sentry:
   ```bash
   npm install @sentry/react
   ```

2. Создайте проект на [sentry.io](https://sentry.io)

3. Добавьте DSN в `.env.production`:
   ```env
   VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id
   ```

4. Пересоберите проект:
   ```bash
   npm run build
   ```

---

**Проект работает без Sentry, но для production мониторинга рекомендуется его установить.**


