# 🚀 Текущие шаги деплоя

## ✅ Что уже сделано:
- Node.js установлен (v18.19.1) - но нужно обновить до 20+
- Nginx установлен
- Git установлен

## 📋 Следующие команды (выполняйте по порядку):

### Шаг 1: Установка npm
```bash
apt install npm
```

### Шаг 2: Обновление Node.js до версии 20+
```bash
# Установка NodeSource репозитория для Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -

# Обновление Node.js
apt-get install -y nodejs

# Проверка версии (должна быть 20+)
node --version
npm --version
```

### Шаг 3: Установка PM2
```bash
npm install -g pm2
```

### Шаг 4: Проверка установки
```bash
node --version  # Должно быть v20.x.x или выше
npm --version
pm2 --version
nginx -v
```

### Шаг 5: Создание директории для проекта
```bash
mkdir -p /var/www
cd /var/www
```

### Шаг 6: Клонирование репозитория
```bash
# Замените <URL_РЕПОЗИТОРИЯ> на реальный URL вашего Git репозитория
git clone <URL_РЕПОЗИТОРИЯ> balansity
cd balansity
```

---

## ⚠️ Важно:
- Вы работаете как root, поэтому `sudo` не нужен
- Порт 80 уже занят - это нормально, Nginx уже работает
- После клонирования репозитория продолжим с настройкой .env файлов




