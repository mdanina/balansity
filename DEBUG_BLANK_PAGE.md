# Отладка белой страницы (blank page)

## 🔍 Диагностика

Если видите **белую страницу** на localhost:8080, выполните следующие шаги:

---

## Шаг 1: Откройте консоль браузера

1. **Нажмите F12** в браузере (или ПКМ → "Проверить элемент")
2. Перейдите на вкладку **"Console"** (Консоль)
3. **Скопируйте все красные ошибки**

### Что искать:

❌ **Ошибки о переменных окружения:**
```
Error: Критические переменные окружения не настроены:
VITE_SUPABASE_URL не установлена
```

❌ **Ошибки импорта:**
```
Failed to resolve import...
Cannot find module...
```

❌ **Ошибки рендера:**
```
Uncaught Error: ...
TypeError: ...
```

---

## Шаг 2: Проверьте .env файл

### Быстрая проверка:

**Windows PowerShell:**
```powershell
Test-Path .env
Test-Path .env.local
```

**Или просто:**
```bash
node check-env-simple.js
```

### Если файла нет:

1. Создайте файл `.env` или `.env.local` в корне проекта
2. Добавьте (значения из `CREATE_ENV_FILE.md`):

```env
VITE_SUPABASE_URL=https://oyuyienekon.beget.app
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY0Mjg4MDAwLCJleHAiOjE5MjIwNTQ0MDB9.tQMuow03daqPpOTnwdMwOma5CNvzVpH1s_pTBxqPdZk
```

3. **Перезапустите dev сервер** (`Ctrl+C` и снова `npm run dev`)

---

## Шаг 3: Проверьте терминал

В терминале, где запущен `npm run dev`, проверьте:

✅ **Хорошо:**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:8080/
```

❌ **Плохо:**
```
Error: ...
Failed to ...
Cannot find module...
```

---

## Шаг 4: Проверьте Network tab

1. В консоли браузера (F12) перейдите на вкладку **"Network"**
2. Обновите страницу (F5)
3. Проверьте запросы:
   - ✅ Должны загружаться файлы `.js`, `.css`
   - ❌ Если есть ошибки (красные) - скопируйте их

---

## Частые причины белой страницы:

### 1. Отсутствует .env файл (самая частая)
**Решение:** Создайте `.env.local` со значениями из `CREATE_ENV_FILE.md`

### 2. Ошибка валидации переменных
**Решение:** Проверьте формат в `.env` файле

### 3. Ошибка импорта модулей
**Решение:** Выполните `npm install`

### 4. Ошибка в коде
**Решение:** Проверьте консоль браузера на ошибки

---

## 🔧 Временное решение (для диагностики)

Если нужно быстро проверить, можно временно отключить строгую валидацию:

**В `src/lib/supabase.ts` временно закомментируйте:**

```typescript
// Временно для диагностики
// validateEnv();
```

Но **не забудьте вернуть обратно** после исправления проблемы!

---

## 📋 Что сообщить для помощи:

1. ✅ Текст ошибки из консоли браузера
2. ✅ Текст ошибки из терминала (если есть)
3. ✅ Существует ли файл `.env` или `.env.local`
4. ✅ Содержимое .env файла (можно скрыть ключ: `eyJ...***`)

---

## ✅ После исправления:

1. Убедитесь, что `.env` файл создан
2. Перезапустите dev сервер
3. Очистите кеш браузера (Ctrl+Shift+R)
4. Откройте localhost:8080 снова

---

**Пришлите скриншот консоли браузера (F12 → Console) - и я точно скажу, в чем проблема! 🚀**

