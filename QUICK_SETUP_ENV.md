# ⚡ Быстрая настройка .env для локального Supabase

## 🎯 Что нужно сделать

Создайте файл `supabase/.env` со следующим содержимым:

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=http://localhost:8080
```

## 📝 Пошаговая инструкция

### Способ 1: Через командную строку (PowerShell)

```powershell
# Перейдите в папку supabase
cd supabase

# Создайте файл .env
New-Item -Path .env -ItemType File -Force

# Добавьте содержимое
Set-Content -Path .env -Value "GOTRUE_MAILER_AUTOCONFIRM=true`nGOTRUE_SITE_URL=http://localhost:8080"
```

### Способ 2: Вручную

1. Откройте папку `supabase/` в проводнике
2. Создайте новый текстовый файл
3. Назовите его `.env` (с точкой в начале!)
4. Откройте в блокноте и вставьте:

```
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=http://localhost:8080
```

5. Сохраните файл

### Способ 3: Скопировать из примера

```powershell
# Скопируйте пример файла
Copy-Item supabase/env.local.example supabase/.env
```

## ✅ После создания файла

1. **Перезапустите Supabase:**
   ```bash
   # Если используете Docker Compose
   docker-compose restart auth
   
   # Если используете Supabase CLI
   supabase stop && supabase start
   ```

2. **Проверьте работу:**
   - Зарегистрируйте нового пользователя
   - Он должен сразу авторизоваться без подтверждения email

## 📚 Дополнительная информация

- Подробная инструкция: `CREATE_SUPABASE_ENV.md`
- Пример файла: `supabase.env.example`













