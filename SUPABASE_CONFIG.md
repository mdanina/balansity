# 🔧 Настройка Supabase на Beget

## 📋 Какие данные нужны из Supabase

### 1. Project URL (URL проекта)
Это адрес вашего Supabase проекта.

**Где найти:**
1. Войдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Найдите раздел **Project URL**
5. Скопируйте URL (пример: `https://xxxxx.supabase.co`)

### 2. Anon Key (Публичный ключ)
Это публичный ключ для доступа к API с фронтенда.

**Где найти:**
1. В том же разделе **Settings** → **API**
2. Найдите раздел **Project API keys**
3. Скопируйте **anon public** ключ
4. ⚠️ **НЕ копируйте** `service_role` ключ (он секретный!)

---

## 🔐 Настройка переменных окружения

### Шаг 1: Создайте файл `.env.local`

В корне проекта создайте файл `.env.local` (если его еще нет):

```env
VITE_SUPABASE_URL=https://ваш-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-anon-key-здесь
```

### Шаг 2: Замените значения

Замените:
- `https://ваш-project-id.supabase.co` на ваш Project URL
- `ваш-anon-key-здесь` на ваш anon public ключ

**Пример:**
```env
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYxNjIzOTAyMiwiZXhwIjoxOTMxODE1MDIyfQ.пример-ключа
```

### Шаг 3: Проверьте .gitignore

Убедитесь, что `.env.local` в `.gitignore` (чтобы не закоммитить секретные ключи):

```gitignore
.env.local
.env*.local
```

---

## 🗄️ Применение миграций SQL

### Шаг 1: Откройте SQL Editor

1. В Supabase Dashboard выберите ваш проект
2. В левом меню найдите **SQL Editor**
3. Нажмите **New query**

### Шаг 2: Примените миграции по порядку

Выполните миграции **строго по порядку**:

#### Миграция 1: Создание таблиц
1. Откройте файл `supabase/migrations/001_initial_schema.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor в Supabase
4. Нажмите **Run** (или `Ctrl+Enter`)
5. Дождитесь сообщения "Success"

#### Миграция 2: Настройка безопасности (RLS)
1. Откройте файл `supabase/migrations/002_rls_policies.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь сообщения "Success"

#### Миграция 3: Функции для подсчета баллов
1. Откройте файл `supabase/migrations/003_scoring_functions.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь сообщения "Success"

---

## ✅ Проверка настройки

### Проверка 1: Таблицы созданы

1. В Supabase Dashboard перейдите в **Table Editor**
2. Должны быть видны таблицы:
   - ✅ `users`
   - ✅ `profiles`
   - ✅ `assessments`
   - ✅ `answers`

### Проверка 2: RLS включен

1. В **Table Editor** выберите любую таблицу
2. Перейдите на вкладку **Policies**
3. Должны быть видны политики безопасности

### Проверка 3: Функции созданы

1. В SQL Editor выполните:
```sql
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'public' 
AND routine_type = 'FUNCTION';
```

Должны быть видны:
- ✅ `get_active_assessment`
- ✅ `complete_assessment`
- ✅ `calculate_checkup_scores`
- ✅ `update_updated_at_column`
- ✅ `handle_new_user`

---

## 🚀 Установка зависимостей

После настройки переменных окружения установите Supabase клиент:

```bash
npm install @supabase/supabase-js
```

---

## 🧪 Тест подключения

Создайте тестовый файл для проверки (опционально):

```typescript
// test-connection.ts
import { supabase } from './src/lib/supabase';

async function testConnection() {
  try {
    const { data, error } = await supabase.from('users').select('count');
    if (error) {
      console.error('Ошибка подключения:', error);
    } else {
      console.log('✅ Подключение к Supabase успешно!');
    }
  } catch (error) {
    console.error('Ошибка:', error);
  }
}

testConnection();
```

---

## ⚠️ Важные замечания

1. **Не коммитьте `.env.local`** в Git!
2. **Anon Key безопасен** для использования на фронтенде (RLS защищает данные)
3. **Service Role Key** - НИКОГДА не используйте на фронтенде!
4. После изменения `.env.local` **перезапустите dev сервер**

---

## 📞 Если что-то не работает

### Ошибка подключения
- Проверьте правильность URL и ключа
- Убедитесь, что проект активен в Supabase
- Проверьте консоль браузера на ошибки

### Ошибки при выполнении SQL
- Проверьте, что миграции выполнены по порядку
- Убедитесь, что нет синтаксических ошибок
- Проверьте логи в Supabase Dashboard

### RLS блокирует запросы
- Убедитесь, что пользователь авторизован
- Проверьте политики безопасности
- Проверьте, что `auth.uid()` возвращает ID пользователя

---

## 📝 Чеклист

- [ ] Получен Project URL из Supabase
- [ ] Получен Anon Key из Supabase
- [ ] Создан файл `.env.local`
- [ ] Добавлены переменные окружения
- [ ] Применена миграция 001 (таблицы)
- [ ] Применена миграция 002 (RLS)
- [ ] Применена миграция 003 (функции)
- [ ] Проверены таблицы в Table Editor
- [ ] Установлен `@supabase/supabase-js`
- [ ] Перезапущен dev сервер

---

Готово! После выполнения всех шагов приложение будет подключено к Supabase! 🎉

