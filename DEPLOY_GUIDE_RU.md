# 🚀 Полное руководство по деплою в продакшн

## 📋 Содержание

1. [Подготовка сервера](#1-подготовка-сервера)
2. [Подготовка проекта](#2-подготовка-проекта)
3. [Настройка переменных окружения](#3-настройка-переменных-окружения)
4. [Деплой фронтенда](#4-деплой-фронтенда)
5. [Деплой Queue Worker](#5-деплой-queue-worker)
6. [Настройка Nginx](#6-настройка-nginx)
7. [Настройка SSL (HTTPS)](#7-настройка-ssl-https)
8. [Проверка и тестирование](#8-проверка-и-тестирование)
9. [Мониторинг и обслуживание](#9-мониторинг-и-обслуживание)

---

## 1. Подготовка сервера

### 1.1 Подключение к серверу

```bash
# Подключитесь к вашему серверу
ssh user@ваш-сервер.com
# или
ssh user@IP-адрес-сервера
```

### 1.2 Установка необходимого ПО

Проверьте, что установлено:

```bash
# Проверка Node.js (нужна версия 20+)
node --version

# Если не установлен, установите:
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка npm
npm --version

# Установка PM2 (для управления процессами)
sudo npm install -g pm2

# Проверка Nginx
nginx -v

# Если не установлен:
sudo apt-get update
sudo apt-get install -y nginx

# Проверка Git
git --version

# Если не установлен:
sudo apt-get install -y git
```

### 1.3 Создание директории для проекта

```bash
# Создайте директорию для проектов (если еще нет)
sudo mkdir -p /var/www
sudo chown -R $USER:$USER /var/www
```

---

## 2. Подготовка проекта

### 2.1 Клонирование репозитория

```bash
# Перейдите в директорию для проектов
cd /var/www

# Клонируйте репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> balansity

# Перейдите в директорию проекта
cd balansity

# Проверьте, что все файлы на месте
ls -la
```

**Важно:** Замените `<URL_ВАШЕГО_РЕПОЗИТОРИЯ>` на реальный URL вашего Git репозитория.

---

## 3. Настройка переменных окружения

### 3.1 Получение ключей Supabase

**Где найти ключи Supabase:**

1. Войдите в админ-панель Supabase: `https://oyuyienekon.beget.app`
   - Логин: `user`
   - Пароль: `G3%DvQmA`

2. Перейдите в **Settings** → **API**

3. Скопируйте:
   - **Project URL**: `https://oyuyienekon.beget.app` (или с портом `:8000`)
   - **anon public key** - для фронтенда
   - **service_role key** - для queue-worker (⚠️ секретный!)

### 3.2 Создание .env.production для фронтенда

```bash
# В корне проекта
cd /var/www/balansity

# Создайте файл .env.production
nano .env.production
```

**Содержимое файла:**

```env
# Supabase конфигурация
VITE_SUPABASE_URL=https://oyuyienekon.beget.app
VITE_SUPABASE_ANON_KEY=ваш-anon-key-здесь

# API URL для платежей (queue-worker)
# Замените на ваш домен
VITE_API_URL=https://ваш-домен.com/api
```

**Важно:**
- Замените `ваш-anon-key-здесь` на реальный anon key из Supabase
- Замените `ваш-домен.com` на ваш реальный домен (например, `balansity.ru`)

**Сохраните файл:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### 3.3 Создание .env для queue-worker

```bash
# Перейдите в директорию queue-worker
cd /var/www/balansity/queue-worker

# Создайте файл .env
nano .env
```

**Содержимое файла:**

```env
# Supabase конфигурация
SUPABASE_URL=https://oyuyienekon.beget.app
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key-здесь

# API Server
API_PORT=3001
API_BASE_URL=https://ваш-домен.com
FRONTEND_URL=https://ваш-домен.com

# YooKassa (ПРОДАКШН ключи!)
YUKASSA_SHOP_ID=ваш-shop-id
YUKASSA_SECRET_KEY=ваш-секретный-ключ

# SMTP настройки
EMAIL_PROVIDER=smtp
SMTP_HOST=mail.nic.ru
SMTP_PORT=587
SMTP_USER=noreply@balansity.ru
SMTP_PASS=ваш-пароль-здесь
SMTP_FROM=noreply@balansity.ru
SMTP_FROM_NAME=Little Otter

# Настройки worker
WORKER_INTERVAL_MS=5000
MAX_TASKS_PER_CYCLE=10
LOG_LEVEL=info
```

**Важно:**
- Замените `ваш-service-role-key-здесь` на реальный service_role key
- Замените `ваш-shop-id` и `ваш-секретный-ключ` на продакшн ключи ЮKassa
- Замените `ваш-пароль-здесь` на пароль от почтового ящика
- Замените `ваш-домен.com` на ваш реальный домен

**Сохраните файл:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

---

## 4. Деплой фронтенда

### 4.1 Установка зависимостей

```bash
# Вернитесь в корень проекта
cd /var/www/balansity

# Установите зависимости
npm install
```

### 4.2 Сборка проекта

```bash
# Соберите проект для продакшена
npm run build

# Проверьте, что папка dist создана
ls -la dist

# Должны увидеть файлы: index.html, assets/, и т.д.
```

**Если сборка прошла успешно**, вы увидите сообщение о том, что файлы собраны в папку `dist`.

---

## 5. Деплой Queue Worker

### 5.1 Установка зависимостей

```bash
# Перейдите в директорию queue-worker
cd /var/www/balansity/queue-worker

# Установите зависимости
npm install
```

### 5.2 Сборка проекта

```bash
# Соберите TypeScript проект
npm run build

# Проверьте, что папка dist создана
ls -la dist

# Должен быть файл: dist/index.js
```

### 5.3 Создание папки для логов

```bash
# Создайте папку для логов
mkdir -p logs
```

### 5.4 Запуск через PM2

```bash
# Запустите через PM2
npm run start:pm2

# Проверьте статус
pm2 status

# Должны увидеть процесс "balansity-queue-worker" в статусе "online"

# Посмотрите логи
pm2 logs balansity-queue-worker

# Если все хорошо, остановите просмотр логов: Ctrl+C

# Сохраните конфигурацию PM2 (чтобы процесс запускался после перезагрузки)
pm2 save

# Настройте автозапуск при загрузке системы
pm2 startup
# Выполните команду, которую покажет PM2 (обычно что-то вроде: sudo env PATH=...)
```

---

## 6. Настройка Nginx

### 6.1 Создание конфигурации Nginx

```bash
# Создайте конфигурационный файл
sudo nano /etc/nginx/sites-available/balansity
```

**Содержимое файла:**

```nginx
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    # Редирект на HTTPS (раскомментируйте после настройки SSL)
    # return 301 https://$server_name$request_uri;

    # Корневая директория со статическими файлами фронтенда
    root /var/www/balansity/dist;
    index index.html;

    # Логи
    access_log /var/log/nginx/balansity-access.log;
    error_log /var/log/nginx/balansity-error.log;

    # Основная локация - SPA роутинг
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Проксирование API запросов к queue-worker
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты для долгих запросов (webhook)
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Блокировка доступа к скрытым файлам
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
```

**Важно:** Замените `ваш-домен.com` на ваш реальный домен (например, `balansity.ru`)

**Сохраните файл:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### 6.2 Активация конфигурации

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/balansity /etc/nginx/sites-enabled/

# Проверьте конфигурацию на ошибки
sudo nginx -t

# Если все хорошо, вы увидите: "syntax is ok" и "test is successful"

# Перезагрузите Nginx
sudo systemctl reload nginx

# Проверьте статус Nginx
sudo systemctl status nginx
```

---

## 7. Настройка SSL (HTTPS)

### 7.1 Установка Certbot

```bash
# Обновите пакеты
sudo apt-get update

# Установите Certbot
sudo apt-get install -y certbot python3-certbot-nginx
```

### 7.2 Получение SSL сертификата

```bash
# Получите сертификат для вашего домена
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com

# Certbot задаст несколько вопросов:
# 1. Email для уведомлений - введите ваш email
# 2. Согласие с условиями - введите Y
# 3. Редирект HTTP на HTTPS - выберите 2 (Redirect)
```

**Важно:** Замените `ваш-домен.com` на ваш реальный домен

### 7.3 Проверка автоматического обновления

Certbot автоматически настроит обновление сертификата. Проверьте:

```bash
# Проверьте, что таймер обновления активен
sudo systemctl status certbot.timer

# Проверьте, что сертификат будет обновляться автоматически
sudo certbot renew --dry-run
```

### 7.4 Обновление конфигурации Nginx

После получения SSL сертификата, Certbot автоматически обновит конфигурацию Nginx. Проверьте:

```bash
# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

---

## 8. Проверка и тестирование

### 8.1 Проверка фронтенда

1. **Откройте браузер** и перейдите на `https://ваш-домен.com`
2. **Проверьте консоль браузера** (F12 → Console) - не должно быть ошибок
3. **Убедитесь**, что приложение загружается корректно

### 8.2 Проверка Queue Worker

```bash
# Проверьте, что процесс запущен
pm2 status

# Должен быть процесс "balansity-queue-worker" в статусе "online"

# Проверьте логи
pm2 logs balansity-queue-worker --lines 50

# Проверьте API endpoint локально
curl http://localhost:3001/health

# Должен вернуть: {"status":"ok","service":"balansity-api"}
```

### 8.3 Проверка API через Nginx

```bash
# Проверьте проксирование API
curl https://ваш-домен.com/api/health

# Должен вернуть: {"status":"ok","service":"balansity-api"}
```

### 8.4 Функциональное тестирование

Проверьте основные функции:

1. **Регистрация/Вход**
   - Зарегистрируйте нового пользователя
   - Войдите в систему
   - Проверьте, что профиль загружается

2. **Создание платежа**
   - Создайте тестовый платеж
   - Проверьте, что редирект на ЮKassa работает
   - Проверьте логи queue-worker

3. **Отправка email**
   - Проверьте, что email отправляются после регистрации
   - Проверьте логи: `pm2 logs balansity-queue-worker | grep -i email`

4. **Обработка webhook**
   - Проверьте, что webhook от ЮKassa обрабатывается
   - Проверьте логи: `pm2 logs balansity-queue-worker | grep -i webhook`

---

## 9. Мониторинг и обслуживание

### 9.1 Мониторинг процессов

```bash
# Статус PM2
pm2 status

# Мониторинг в реальном времени
pm2 monit

# Логи
pm2 logs balansity-queue-worker

# Остановить просмотр логов: Ctrl+C
```

### 9.2 Логи Nginx

```bash
# Логи доступа
sudo tail -f /var/log/nginx/balansity-access.log

# Логи ошибок
sudo tail -f /var/log/nginx/balansity-error.log

# Остановить просмотр: Ctrl+C
```

### 9.3 Обновление проекта

#### Обновление фронтенда:

```bash
cd /var/www/balansity

# Получите последние изменения
git pull origin main

# Установите новые зависимости (если есть)
npm install

# Пересоберите проект
npm run build

# Nginx автоматически будет использовать новые файлы
```

#### Обновление Queue Worker:

```bash
cd /var/www/balansity/queue-worker

# Получите последние изменения
git pull origin main

# Установите новые зависимости
npm install

# Пересоберите
npm run build

# Перезапустите через PM2
npm run restart:pm2

# Или вручную:
pm2 restart balansity-queue-worker
```

---

## 🚨 Решение проблем

### Проблема: Фронтенд не загружается

**Решение:**
```bash
# 1. Проверьте, что Nginx запущен
sudo systemctl status nginx

# 2. Проверьте конфигурацию
sudo nginx -t

# 3. Проверьте права доступа к папке dist
ls -la /var/www/balansity/dist

# 4. Проверьте логи
sudo tail -f /var/log/nginx/balansity-error.log
```

### Проблема: API не отвечает

**Решение:**
```bash
# 1. Проверьте, что Queue Worker запущен
pm2 status

# 2. Проверьте порт
netstat -tulpn | grep 3001

# 3. Проверьте логи
pm2 logs balansity-queue-worker

# 4. Проверьте переменные окружения
cat /var/www/balansity/queue-worker/.env
```

### Проблема: Ошибки подключения к Supabase

**Решение:**
```bash
# 1. Проверьте переменные в .env.production
cat /var/www/balansity/.env.production

# 2. Убедитесь, что Supabase доступен
curl https://oyuyienekon.beget.app

# 3. Проверьте ключи в админ-панели Supabase
```

### Проблема: Email не отправляются

**Решение:**
```bash
# 1. Проверьте SMTP настройки
cat /var/www/balansity/queue-worker/.env | grep SMTP

# 2. Проверьте логи
pm2 logs balansity-queue-worker | grep -i email

# 3. Убедитесь, что почтовый ящик существует и пароль правильный
```

### Проблема: Платежи не обрабатываются

**Решение:**
```bash
# 1. Проверьте ключи ЮKassa
cat /var/www/balansity/queue-worker/.env | grep YUKASSA

# 2. Проверьте, что webhook URL правильный
# Должен быть: https://ваш-домен.com/api/webhook

# 3. Проверьте логи webhook
pm2 logs balansity-queue-worker | grep -i webhook
```

---

## ✅ Чек-лист деплоя

Используйте этот чек-лист для проверки всех этапов:

### Подготовка
- [ ] Сервер подготовлен (Node.js, npm, PM2, Nginx, Git)
- [ ] Репозиторий склонирован на сервер
- [ ] Получены все необходимые ключи (Supabase, ЮKassa, SMTP)

### Фронтенд
- [ ] `.env.production` создан и заполнен
- [ ] Зависимости установлены (`npm install`)
- [ ] Проект собран (`npm run build`)
- [ ] Папка `dist` создана и содержит файлы

### Queue Worker
- [ ] `.env` создан и заполнен
- [ ] Зависимости установлены (`npm install`)
- [ ] Проект собран (`npm run build`)
- [ ] Запущен через PM2 (`npm run start:pm2`)
- [ ] PM2 сохранен (`pm2 save`)
- [ ] Автозапуск настроен (`pm2 startup`)

### Nginx
- [ ] Конфигурация создана (`/etc/nginx/sites-available/balansity`)
- [ ] Символическая ссылка создана
- [ ] Конфигурация проверена (`nginx -t`)
- [ ] Nginx перезагружен

### SSL
- [ ] Certbot установлен
- [ ] SSL сертификат получен
- [ ] HTTPS работает
- [ ] HTTP редиректит на HTTPS

### Проверка
- [ ] Фронтенд доступен по домену
- [ ] API доступен через `/api/health`
- [ ] Регистрация работает
- [ ] Платежи работают
- [ ] Email отправляются
- [ ] Webhook обрабатывается

---

## 🔒 Безопасность

### Рекомендации:

1. **Не храните секреты в Git** - используйте `.env` файлы, которые в `.gitignore`
2. **Используйте HTTPS** - настройте SSL сертификат
3. **Ограничьте доступ** - используйте firewall для закрытия ненужных портов
4. **Регулярно обновляйте** - обновляйте зависимости и систему
5. **Мониторинг** - следите за логами на предмет подозрительной активности
6. **Резервное копирование** - настройте регулярные бэкапы базы данных

### Настройка Firewall (опционально):

```bash
# Установите UFW (если еще не установлен)
sudo apt-get install -y ufw

# Разрешите SSH
sudo ufw allow ssh

# Разрешите HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включите firewall
sudo ufw enable

# Проверьте статус
sudo ufw status
```

---

## 📞 Полезные команды

### PM2
```bash
pm2 status                    # Статус процессов
pm2 logs balansity-queue-worker  # Логи
pm2 restart balansity-queue-worker  # Перезапуск
pm2 stop balansity-queue-worker     # Остановка
pm2 delete balansity-queue-worker   # Удаление
```

### Nginx
```bash
sudo nginx -t                # Проверка конфигурации
sudo systemctl reload nginx  # Перезагрузка
sudo systemctl restart nginx # Перезапуск
sudo systemctl status nginx  # Статус
```

### Git
```bash
git pull origin main         # Получить обновления
git status                   # Статус изменений
git log --oneline -10        # Последние коммиты
```

---

**Удачи с деплоем! 🚀**

Если возникли проблемы, проверьте раздел "Решение проблем" или логи соответствующих сервисов.

