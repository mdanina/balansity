# 🔧 Решение проблемы: Не приходят письма подтверждения регистрации на продакшене

## 🎯 Проблема

На продакшн сервере не приходят письма подтверждения регистрации для новых пользователей. Локально все работало.

## 🔍 Пошаговая диагностика

### Шаг 1: Проверка переменных окружения в контейнере auth

**Важно:** Проверьте, что переменные окружения действительно применены в контейнере:

```bash
# Подключитесь к серверу по SSH
ssh user@ваш-сервер

# Найдите путь к docker-compose.yml Supabase
# Обычно это: /opt/beget/supabase/ или /home/user/supabase/

# Проверьте переменные окружения в контейнере auth
docker compose -f /opt/beget/supabase/docker-compose.yml exec auth env | grep GOTRUE
```

**Что должно быть:**

```bash
GOTRUE_MAILER_AUTOCONFIRM=false          # false = требуется подтверждение email
GOTRUE_SITE_URL=https://ваш-домен.com
GOTRUE_SMTP_HOST=smtp.beget.com          # или ваш SMTP хост
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Little Otter
```

**Если переменных нет или они неправильные** → переходите к Шагу 2.

### Шаг 2: Проверка файла .env на сервере

Найдите файл `.env` в папке с Supabase (обычно `/opt/beget/supabase/.env`):

```bash
cat /opt/beget/supabase/.env | grep -E "GOTRUE|SMTP|MAILER"
```

**Что должно быть в .env:**

```env
# Для продакшена (с подтверждением email)
GOTRUE_MAILER_AUTOCONFIRM=false
GOTRUE_SITE_URL=https://ваш-домен.com
GOTRUE_URI_ALLOW_LIST=https://ваш-домен.com/email-confirm,https://ваш-домен.com

# Настройки SMTP
GOTRUE_SMTP_HOST=smtp.beget.com
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Little Otter
GOTRUE_MAILER_URLPATHS_CONFIRMATION=/email-confirm
```

**⚠️ ВАЖНО:** 
- Если в `.env` переменные БЕЗ префикса `GOTRUE_` (например, `SMTP_HOST`), проверьте `docker-compose.yml` - там они должны подставляться с префиксом
- Если переменные с префиксом `GOTRUE_` в `.env`, они должны использоваться напрямую

### Шаг 3: Проверка docker-compose.yml

Проверьте, как переменные используются в `docker-compose.yml`:

```bash
cat /opt/beget/supabase/docker-compose.yml | grep -A 20 "auth:" | grep -E "environment|GOTRUE|SMTP"
```

**Вариант 1: Если переменные в .env БЕЗ префикса GOTRUE_**

В `docker-compose.yml` должно быть:

```yaml
services:
  auth:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "false"
      GOTRUE_SITE_URL: "${SITE_URL}"
      GOTRUE_SMTP_HOST: "${SMTP_HOST}"
      GOTRUE_SMTP_PORT: "${SMTP_PORT}"
      GOTRUE_SMTP_USER: "${SMTP_USER}"
      GOTRUE_SMTP_PASS: "${SMTP_PASS}"
      GOTRUE_SMTP_ADMIN_EMAIL: "${SMTP_ADMIN_EMAIL}"
      GOTRUE_SMTP_SENDER_NAME: "${SMTP_SENDER_NAME}"
```

А в `.env`:

```env
SITE_URL=https://ваш-домен.com
SMTP_HOST=smtp.beget.com
SMTP_PORT=587
SMTP_USER=ваш-email@beget.com
SMTP_PASS=ваш-пароль
SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
SMTP_SENDER_NAME=Little Otter
```

**Вариант 2: Если переменные в .env С префиксом GOTRUE_**

В `docker-compose.yml` должно быть:

```yaml
services:
  auth:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "false"
      GOTRUE_SITE_URL: "${GOTRUE_SITE_URL}"
      GOTRUE_SMTP_HOST: "${GOTRUE_SMTP_HOST}"
      GOTRUE_SMTP_PORT: "${GOTRUE_SMTP_PORT}"
      GOTRUE_SMTP_USER: "${GOTRUE_SMTP_USER}"
      GOTRUE_SMTP_PASS: "${GOTRUE_SMTP_PASS}"
      GOTRUE_SMTP_ADMIN_EMAIL: "${GOTRUE_SMTP_ADMIN_EMAIL}"
      GOTRUE_SMTP_SENDER_NAME: "${GOTRUE_SMTP_SENDER_NAME}"
```

А в `.env`:

```env
GOTRUE_MAILER_AUTOCONFIRM=false
GOTRUE_SITE_URL=https://ваш-домен.com
GOTRUE_SMTP_HOST=smtp.beget.com
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Little Otter
```

### Шаг 4: Проверка логов контейнера auth

Проверьте логи на наличие ошибок отправки email:

```bash
# Последние 100 строк логов
docker compose -f /opt/beget/supabase/docker-compose.yml logs --tail=100 auth | grep -i "mail\|smtp\|email\|error"

# Или все логи за последний час
docker compose -f /opt/beget/supabase/docker-compose.yml logs --since=1h auth
```

**Что искать:**
- Ошибки подключения к SMTP: `connection refused`, `timeout`, `authentication failed`
- Ошибки отправки: `send mail error`, `mailer error`
- Предупреждения о неправильных настройках

### Шаг 5: Тест подключения к SMTP

Проверьте, доступен ли SMTP сервер:

```bash
# Тест подключения к SMTP (замените на ваши данные)
telnet smtp.beget.com 587

# Или через openssl для TLS
openssl s_client -connect smtp.beget.com:587 -starttls smtp
```

Если подключение не удается, проблема в сетевых настройках или блокировке портов.

## ✅ Решение проблемы

### Решение 1: Исправить переменные окружения

1. **Отредактируйте файл `.env`:**

```bash
nano /opt/beget/supabase/.env
```

Добавьте/измените (для продакшена с подтверждением email):

```env
# Отключить автоподтверждение (требовать подтверждение email)
GOTRUE_MAILER_AUTOCONFIRM=false

# URL вашего сайта
GOTRUE_SITE_URL=https://ваш-домен.com

# Разрешенные URL для редиректов
GOTRUE_URI_ALLOW_LIST=https://ваш-домен.com/email-confirm,https://ваш-домен.com

# Настройки SMTP
GOTRUE_SMTP_HOST=smtp.beget.com
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Little Otter
GOTRUE_MAILER_URLPATHS_CONFIRMATION=/email-confirm
```

2. **Проверьте `docker-compose.yml`** - убедитесь, что переменные правильно подставляются

3. **Перезапустите контейнер auth:**

```bash
cd /opt/beget/supabase
docker compose restart auth

# Или полный перезапуск
docker compose down && docker compose up -d
```

4. **Проверьте, что переменные применились:**

```bash
docker compose exec auth env | grep GOTRUE
```

### Решение 2: Если переменные не применяются из .env

Если переменные из `.env` не применяются, добавьте их напрямую в `docker-compose.yml`:

```yaml
services:
  auth:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "false"
      GOTRUE_SITE_URL: "https://ваш-домен.com"
      GOTRUE_URI_ALLOW_LIST: "https://ваш-домен.com/email-confirm,https://ваш-домен.com"
      GOTRUE_SMTP_HOST: "smtp.beget.com"
      GOTRUE_SMTP_PORT: "587"
      GOTRUE_SMTP_USER: "ваш-email@beget.com"
      GOTRUE_SMTP_PASS: "ваш-пароль"
      GOTRUE_SMTP_ADMIN_EMAIL: "noreply@ваш-домен.com"
      GOTRUE_SMTP_SENDER_NAME: "Little Otter"
      GOTRUE_MAILER_URLPATHS_CONFIRMATION: "/email-confirm"
```

Затем перезапустите:

```bash
docker compose down && docker compose up -d
```

### Решение 3: Проверка SMTP учетных данных

1. **Убедитесь, что SMTP учетные данные правильные:**
   - Проверьте логин и пароль в панели управления хостингом
   - Убедитесь, что email существует и активен
   - Проверьте, что SMTP сервер доступен с вашего сервера

2. **Проверьте настройки SMTP:**
   - Порт: обычно `587` (TLS) или `465` (SSL)
   - Хост: `smtp.beget.com` для Beget
   - Для других провайдеров уточните настройки

3. **Протестируйте отправку email вручную:**

```bash
# Установите mailutils (если нет)
apt-get install mailutils

# Отправьте тестовое письмо
echo "Test email" | mail -s "Test" ваш-email@example.com
```

### Решение 4: Проверка сетевых настроек

Если SMTP недоступен:

1. **Проверьте firewall:**
```bash
# Проверьте, открыт ли порт 587
netstat -tuln | grep 587

# Или через iptables
iptables -L -n | grep 587
```

2. **Проверьте DNS:**
```bash
nslookup smtp.beget.com
```

3. **Проверьте доступность SMTP:**
```bash
telnet smtp.beget.com 587
```

## 🧪 Тестирование после исправления

1. **Проверьте переменные окружения:**
```bash
docker compose exec auth env | grep GOTRUE
```

2. **Проверьте логи при регистрации:**
```bash
# В одном терминале следите за логами
docker compose logs -f auth

# В другом терминале или в браузере зарегистрируйте тестового пользователя
```

3. **Проверьте почту:**
   - Проверьте папку "Входящие"
   - Проверьте папку "Спам"
   - Убедитесь, что письмо содержит ссылку для подтверждения

## 🚨 Частые проблемы и решения

### Проблема 1: `GOTRUE_MAILER_AUTOCONFIRM=true` вместо `false`

**Симптом:** Пользователи регистрируются без подтверждения email

**Решение:** Установите `GOTRUE_MAILER_AUTOCONFIRM=false` и перезапустите контейнер

### Проблема 2: SMTP authentication failed

**Симптом:** В логах ошибка `authentication failed`

**Решение:** 
- Проверьте правильность логина и пароля
- Убедитесь, что используется правильный формат email для `GOTRUE_SMTP_USER`
- Проверьте, что пароль не содержит специальных символов, которые нужно экранировать

### Проблема 3: Connection refused / timeout

**Симптом:** В логах ошибка подключения к SMTP

**Решение:**
- Проверьте доступность SMTP сервера
- Проверьте firewall на сервере
- Убедитесь, что порт правильный (587 для TLS, 465 для SSL)

### Проблема 4: Переменные не применяются после перезапуска

**Симптом:** Переменные в контейнере не соответствуют `.env`

**Решение:**
- Убедитесь, что `docker-compose.yml` правильно ссылается на переменные из `.env`
- Используйте `docker compose down && docker compose up -d` вместо `restart`
- Проверьте, что файл `.env` находится в той же папке, что и `docker-compose.yml`

### Проблема 5: Письма уходят в спам

**Симптом:** Письма не приходят во входящие, но есть в спаме

**Решение:**
- Настройте SPF, DKIM, DMARC записи для вашего домена
- Используйте email с вашего домена (например, `noreply@ваш-домен.com`)
- Убедитесь, что `GOTRUE_SMTP_ADMIN_EMAIL` соответствует домену

## 📋 Чеклист для проверки

- [ ] `GOTRUE_MAILER_AUTOCONFIRM=false` (включено подтверждение)
- [ ] `GOTRUE_SITE_URL` указывает на правильный домен продакшена
- [ ] Все SMTP переменные заполнены и правильные
- [ ] Переменные окружения видны в контейнере (`docker compose exec auth env | grep GOTRUE`)
- [ ] Контейнер auth перезапущен после изменений
- [ ] SMTP сервер доступен (проверено через telnet/openssl)
- [ ] Логи не показывают ошибок отправки email
- [ ] Тестовое письмо отправлено и получено

## 📞 Если ничего не помогает

1. **Проверьте документацию GoTrue:**
   - https://github.com/supabase/gotrue
   - Все переменные окружения начинаются с `GOTRUE_`

2. **Обратитесь в поддержку хостинга:**
   - Уточните настройки SMTP
   - Проверьте, не блокируются ли исходящие соединения на порт 587

3. **Временное решение:**
   - Можно временно отключить подтверждение email (`GOTRUE_MAILER_AUTOCONFIRM=true`)
   - Но для продакшена это не рекомендуется из соображений безопасности

## 🚀 Быстрая проверка (автоматическая)

Используйте скрипт для автоматической проверки настроек:

```bash
# На сервере
node check-email-config.js --docker-compose /opt/beget/supabase/docker-compose.yml
```

Скрипт проверит:
- ✅ Наличие всех необходимых переменных
- ✅ Правильность значений
- ✅ Соответствие между .env и контейнером
- ✅ Ошибки в логах

## 🔗 Связанные документы

- `SUPABASE_SELF_HOSTED_EMAIL_SETUP.md` - Подробная настройка email
- `QUICK_EMAIL_SETUP.md` - Быстрая настройка
- `SMTP_SETUP_BALANSITY.md` - Пример настройки для конкретного домена
- `FIX_REGISTRATION_ISSUE.md` - Решение проблем с регистрацией
- `check-email-config.js` - Скрипт для автоматической проверки настроек
