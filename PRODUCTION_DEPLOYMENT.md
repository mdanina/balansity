# 🚀 Инструкция по деплою проекта в продакшн

## 📋 Обзор проекта

Проект состоит из трех основных компонентов:

1. **Фронтенд** - React приложение (Vite)
2. **Queue Worker** - Node.js сервер для обработки очередей и API платежей
3. **Supabase** - База данных (уже установлен на сервере)

---

## 🎯 Подготовка к деплою

### Шаг 1: Подготовка сервера

Убедитесь, что на сервере установлено:
- Node.js (версия 20 или выше)
- npm или yarn
- PM2 (для управления процессами) - `npm install -g pm2`
- Nginx (для проксирования и статики)
- Git

### Шаг 2: Клонирование репозитория

```bash
# Подключитесь к серверу
ssh user@ваш-сервер.com

# Перейдите в директорию для проектов (например, /var/www)
cd /var/www

# Клонируйте репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> balansity
cd balansity
```

---

## 🔧 Часть 1: Деплой фронтенда

### Шаг 1.1: Установка зависимостей и сборка

```bash
# Установите зависимости
npm install

# Создайте файл .env.production для продакшена
nano .env.production
```

### Шаг 1.2: Настройка переменных окружения

Создайте файл `.env.production` в корне проекта со следующим содержимым:

```env
# Supabase конфигурация
VITE_SUPABASE_URL=https://oyuyienekon.beget.app
VITE_SUPABASE_ANON_KEY=ваш-anon-key-здесь

# API URL для платежей (queue-worker)
# Замените на ваш домен
VITE_API_URL=https://ваш-домен.com/api
```

**Важно:** 
- Замените `ваш-anon-key-здесь` на реальный anon key из Supabase
- Замените `ваш-домен.com` на ваш реальный домен

### Шаг 1.3: Сборка проекта

```bash
# Соберите проект для продакшена
npm run build

# Проверьте, что папка dist создана
ls -la dist
```

### Шаг 1.4: Настройка Nginx для фронтенда

Создайте конфигурацию Nginx:

```bash
sudo nano /etc/nginx/sites-available/balansity
```

Добавьте следующую конфигурацию:

```nginx
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    # Редирект на HTTPS (если у вас есть SSL)
    # return 301 https://$server_name$request_uri;

    # Для начала можно использовать HTTP, потом настроите SSL
    root /var/www/balansity/dist;
    index index.html;

    # Логи
    access_log /var/log/nginx/balansity-access.log;
    error_log /var/log/nginx/balansity-error.log;

    # Основная локация
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Статические файлы
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Проксирование API запросов к queue-worker
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируйте конфигурацию:

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/balansity /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

### Шаг 1.5: Настройка SSL (опционально, но рекомендуется)

Если у вас есть SSL сертификат (например, от Let's Encrypt):

```bash
# Установите certbot (если еще не установлен)
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx

# Получите сертификат
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

После этого обновите конфигурацию Nginx, раскомментировав редирект на HTTPS.

---

## ⚙️ Часть 2: Деплой Queue Worker

### Шаг 2.1: Переход в директорию queue-worker

```bash
cd /var/www/balansity/queue-worker
```

### Шаг 2.2: Установка зависимостей

```bash
npm install
```

### Шаг 2.3: Создание .env файла

```bash
nano .env
```

Добавьте следующее содержимое:

```env
# Supabase конфигурация
SUPABASE_URL=https://oyuyienekon.beget.app
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key

# API Server
API_PORT=3001
API_BASE_URL=https://ваш-домен.com
FRONTEND_URL=https://ваш-домен.com

# YooKassa (замените на продакшн ключи!)
YUKASSA_SHOP_ID=ваш-shop-id
YUKASSA_SECRET_KEY=ваш-секретный-ключ

# SMTP настройки
EMAIL_PROVIDER=smtp
SMTP_HOST=mail.nic.ru
SMTP_PORT=587
SMTP_USER=noreply@balansity.ru
SMTP_PASS=ваш-пароль
SMTP_FROM=noreply@balansity.ru
SMTP_FROM_NAME=Little Otter

# Настройки worker
WORKER_INTERVAL_MS=5000
MAX_TASKS_PER_CYCLE=10
LOG_LEVEL=info
```

**Важно:**
- Замените `ваш-service-role-key` на реальный service_role key из Supabase
- Замените `ваш-shop-id` и `ваш-секретный-ключ` на продакшн ключи ЮKassa
- Замените `ваш-пароль` на реальный пароль SMTP
- Замените `ваш-домен.com` на ваш реальный домен

### Шаг 2.4: Сборка проекта

```bash
npm run build
```

### Шаг 2.5: Запуск через PM2

```bash
# Создайте папку для логов
mkdir -p logs

# Запустите через PM2
npm run start:pm2

# Проверьте статус
pm2 status

# Посмотрите логи
pm2 logs balansity-queue-worker

# Сохраните конфигурацию PM2 (чтобы процесс запускался после перезагрузки)
pm2 save
pm2 startup
```

### Шаг 2.6: Альтернатива - Запуск через Docker

Если вы предпочитаете использовать Docker:

```bash
# Убедитесь, что Docker и Docker Compose установлены
docker --version
docker-compose --version

# Соберите образ
docker-compose build

# Запустите контейнер
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

**Примечание:** Если используете Docker, убедитесь, что контейнер может подключиться к Supabase. Возможно, потребуется настроить сеть Docker.

---

## 🔐 Часть 3: Настройка Supabase

### Шаг 3.1: Обновление конфигурации Supabase

Если Supabase установлен на том же сервере, обновите его конфигурацию:

```bash
# Найдите файл .env Supabase (обычно в /opt/supabase/.env или рядом с docker-compose.yml)
find / -name ".env" -path "*/supabase/*" 2>/dev/null

# Отредактируйте файл
nano /opt/supabase/.env  # или путь к вашему файлу
```

Добавьте/обновите следующие переменные:

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=https://ваш-домен.com
```

### Шаг 3.2: Перезапуск Supabase

```bash
# Если используете docker-compose
cd /opt/supabase  # или путь к вашему Supabase
docker-compose restart auth

# Или если используете отдельные контейнеры
docker restart supabase_auth_1
```

### Шаг 3.3: Применение миграций

Убедитесь, что все миграции применены:

```bash
# Подключитесь к базе данных Supabase
# Или используйте Supabase CLI
cd /var/www/balansity
supabase db push  # если используете Supabase CLI

# Или примените миграции вручную через psql или админ-панель Supabase
```

---

## ✅ Часть 4: Проверка и тестирование

### Шаг 4.1: Проверка фронтенда

1. Откройте браузер и перейдите на `https://ваш-домен.com`
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что приложение загружается корректно

### Шаг 4.2: Проверка Queue Worker

```bash
# Проверьте, что процесс запущен
pm2 status

# Проверьте логи
pm2 logs balansity-queue-worker --lines 50

# Проверьте API endpoint
curl http://localhost:3001/health
# Должен вернуть: {"status":"ok","service":"balansity-api"}
```

### Шаг 4.3: Проверка API через Nginx

```bash
# Проверьте проксирование API
curl https://ваш-домен.com/api/health
# Должен вернуть: {"status":"ok","service":"balansity-api"}
```

### Шаг 4.4: Тестирование функционала

1. **Регистрация/Вход** - проверьте авторизацию
2. **Создание платежа** - проверьте интеграцию с ЮKassa
3. **Отправка email** - проверьте отправку уведомлений
4. **Обработка webhook** - проверьте обработку платежей от ЮKassa

---

## 🔄 Часть 5: Обновление проекта

### Шаг 5.1: Обновление фронтенда

```bash
cd /var/www/balansity

# Получите последние изменения
git pull origin main

# Установите новые зависимости (если есть)
npm install

# Пересоберите проект
npm run build

# Перезагрузите Nginx (обычно не требуется, но на всякий случай)
sudo systemctl reload nginx
```

### Шаг 5.2: Обновление Queue Worker

```bash
cd /var/www/balansity/queue-worker

# Получите последние изменения
git pull origin main

# Установите новые зависимости
npm install

# Пересоберите
npm run build

# Перезапустите через PM2
npm run restart:pm2

# Или если используете Docker
docker-compose down
docker-compose build
docker-compose up -d
```

---

## 🛠️ Часть 6: Мониторинг и обслуживание

### Шаг 6.1: Мониторинг процессов

```bash
# Статус PM2
pm2 status

# Мониторинг в реальном времени
pm2 monit

# Логи
pm2 logs balansity-queue-worker
```

### Шаг 6.2: Логи Nginx

```bash
# Логи доступа
sudo tail -f /var/log/nginx/balansity-access.log

# Логи ошибок
sudo tail -f /var/log/nginx/balansity-error.log
```

### Шаг 6.3: Логи Queue Worker

```bash
# Логи через PM2
pm2 logs balansity-queue-worker

# Или напрямую из файлов
tail -f /var/www/balansity/queue-worker/logs/out.log
tail -f /var/www/balansity/queue-worker/logs/error.log
```

### Шаг 6.4: Автоматический перезапуск

PM2 автоматически перезапустит процесс при сбое. Для проверки:

```bash
# Проверьте настройки автозапуска
pm2 startup
pm2 save
```

---

## 🚨 Часть 7: Решение проблем

### Проблема: Фронтенд не загружается

**Решение:**
1. Проверьте, что Nginx запущен: `sudo systemctl status nginx`
2. Проверьте конфигурацию: `sudo nginx -t`
3. Проверьте права доступа к папке `dist`: `ls -la /var/www/balansity/dist`
4. Проверьте логи: `sudo tail -f /var/log/nginx/balansity-error.log`

### Проблема: API не отвечает

**Решение:**
1. Проверьте, что Queue Worker запущен: `pm2 status`
2. Проверьте порт: `netstat -tulpn | grep 3001`
3. Проверьте логи: `pm2 logs balansity-queue-worker`
4. Проверьте переменные окружения в `.env`

### Проблема: Ошибки подключения к Supabase

**Решение:**
1. Проверьте переменные `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` в `.env.production`
2. Убедитесь, что Supabase доступен: `curl https://oyuyienekon.beget.app`
3. Проверьте CORS настройки в Supabase

### Проблема: Email не отправляются

**Решение:**
1. Проверьте SMTP настройки в `.env` queue-worker
2. Проверьте логи: `pm2 logs balansity-queue-worker | grep -i email`
3. Убедитесь, что почтовый ящик существует и пароль правильный

### Проблема: Платежи не обрабатываются

**Решение:**
1. Проверьте ключи ЮKassa в `.env`
2. Проверьте, что webhook URL правильный: `https://ваш-домен.com/api/webhook`
3. Проверьте логи webhook: `pm2 logs balansity-queue-worker | grep -i webhook`

---

## 📝 Чек-лист деплоя

- [ ] Сервер подготовлен (Node.js, npm, PM2, Nginx)
- [ ] Репозиторий склонирован на сервер
- [ ] Фронтенд: `.env.production` создан и заполнен
- [ ] Фронтенд: проект собран (`npm run build`)
- [ ] Фронтенд: Nginx настроен и запущен
- [ ] Queue Worker: `.env` создан и заполнен
- [ ] Queue Worker: проект собран (`npm run build`)
- [ ] Queue Worker: запущен через PM2 или Docker
- [ ] Supabase: конфигурация обновлена
- [ ] Supabase: миграции применены
- [ ] Фронтенд доступен по домену
- [ ] API доступен через `/api/health`
- [ ] Тестирование: регистрация работает
- [ ] Тестирование: платежи работают
- [ ] Тестирование: email отправляются
- [ ] SSL сертификат установлен (опционально)
- [ ] Мониторинг настроен

---

## 🔒 Безопасность

### Рекомендации:

1. **Не храните секреты в Git** - используйте `.env` файлы, которые в `.gitignore`
2. **Используйте HTTPS** - настройте SSL сертификат
3. **Ограничьте доступ** - используйте firewall для закрытия ненужных портов
4. **Регулярно обновляйте** - обновляйте зависимости и систему
5. **Мониторинг** - следите за логами на предмет подозрительной активности
6. **Резервное копирование** - настройте регулярные бэкапы базы данных

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи (см. раздел "Мониторинг")
2. Проверьте переменные окружения
3. Убедитесь, что все сервисы запущены
4. Проверьте сетевые подключения

---

**Удачи с деплоем! 🚀**




