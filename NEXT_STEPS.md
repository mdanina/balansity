# 🚀 Следующие шаги после настройки .env.local

## ✅ Что уже сделано:
- ✅ Получен Project URL: `https://oyuyienekon.beget.app`
- ✅ Получен anon public key
- ✅ Создан файл `.env.local` (нужно создать вручную, см. CREATE_ENV_FILE.md)

---

## 📦 Шаг 1: Установка зависимости

Откройте терминал в папке проекта и выполните:

```bash
npm install @supabase/supabase-js
```

Или если используете другой менеджер пакетов:
```bash
yarn add @supabase/supabase-js
# или
pnpm add @supabase/supabase-js
```

---

## 🗄️ Шаг 2: Применение SQL миграций

### Откройте SQL Editor в Supabase:

1. В админ-панели Supabase найдите **SQL Editor** (Редактор SQL)
   - Обычно в левом меню или по адресу: `https://oyuyienekon.beget.app/project/default/sql`

2. Выполните миграции **строго по порядку**:

### Миграция 1: Создание таблиц

1. Откройте файл `supabase/migrations/001_initial_schema.sql` в вашем проекте
2. Скопируйте **весь** SQL код из файла
3. Вставьте в SQL Editor в Supabase
4. Нажмите **Run** или **Выполнить** (или `Ctrl+Enter`)
5. Дождитесь сообщения "Success" или "Успешно"

### Миграция 2: Настройка безопасности (RLS)

1. Откройте файл `supabase/migrations/002_rls_policies.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь успешного выполнения

### Миграция 3: Функции для подсчета баллов

1. Откройте файл `supabase/migrations/003_scoring_functions.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь успешного выполнения

---

## ✅ Шаг 3: Проверка

### Проверка таблиц:

1. В админ-панели Supabase найдите **Table Editor** (Редактор таблиц)
2. Должны быть видны таблицы:
   - ✅ `users`
   - ✅ `profiles`
   - ✅ `assessments`
   - ✅ `answers`

### Проверка подключения:

1. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

2. Откройте приложение в браузере
3. Откройте консоль браузера (F12 → Console)
4. Не должно быть ошибок типа:
   - "Supabase URL not found"
   - "Failed to connect to Supabase"

---

## ⚠️ Если возникли ошибки

### Ошибка при выполнении SQL:
- Проверьте, что миграции выполнены по порядку (001, 002, 003)
- Убедитесь, что нет синтаксических ошибок
- Проверьте логи в Supabase Dashboard

### Ошибка подключения:
- Убедитесь, что файл `.env.local` создан правильно
- Проверьте, что перезапустили dev сервер после создания `.env.local`
- Проверьте консоль браузера на ошибки CORS

### CORS ошибки:
- Может потребоваться настройка CORS в Supabase
- Обычно это делается в настройках проекта

---

## 🎉 Готово!

После выполнения всех шагов:
- ✅ База данных настроена
- ✅ Таблицы созданы
- ✅ Безопасность настроена (RLS)
- ✅ Функции для подсчета баллов созданы
- ✅ Приложение подключено к Supabase

Теперь можно тестировать приложение! 🚀

---

## 📝 Чеклист

- [ ] Создан файл `.env.local` с правильными данными
- [ ] Установлен `@supabase/supabase-js`
- [ ] Применена миграция 001 (таблицы)
- [ ] Применена миграция 002 (RLS)
- [ ] Применена миграция 003 (функции)
- [ ] Проверены таблицы в Table Editor
- [ ] Перезапущен dev сервер
- [ ] Проверена консоль браузера (нет ошибок)

