# Админ-панель: Быстрый старт

## Шаг 1: Применить миграции БД

Откройте Supabase Dashboard → SQL Editor и выполните по порядку:

1. `supabase/migrations/026_add_user_roles.sql`
2. `supabase/migrations/027_admin_rls_policies.sql`

## Шаг 2: Создать первого админа

**Вариант A: Если пользователь уже зарегистрирован**

В SQL Editor выполните:

```sql
-- Замените email на ваш реальный email
UPDATE public.users 
SET role = 'admin' 
WHERE email = 'your-email@example.com';
```

**Вариант B: Создать нового пользователя**

1. В Supabase Dashboard: **Authentication** → **Users** → **Add User**
2. Введите email и пароль, включите **Auto Confirm User**
3. Затем в SQL Editor выполните UPDATE выше

📖 **Подробная инструкция:** см. `CREATE_FIRST_ADMIN.md`

## Шаг 3: Войти в админ-панель

1. Откройте приложение
2. Перейдите на `/admin/login`
3. Войдите с email и паролем админа
4. Вы будете перенаправлены на дэшборд `/admin`

## Готово! 🎉

Теперь вы можете:
- Просматривать метрики на дэшборде
- Управлять пользователями
- Просматривать оценки и консультации
- Управлять платежами и контентом

---

## Полезные ссылки

- Полная документация: `ADMIN_PANEL_DOCUMENTATION.md`
- Структура проекта: см. раздел "Структура проекта" в документации

## Роли пользователей

- `user` — обычный пользователь (нет доступа к админке)
- `support` — поддержка (есть доступ)
- `admin` — администратор (полный доступ)
- `super_admin` — супер-администратор (полный доступ + управление ролями)

---

**Вопросы?** См. полную документацию `ADMIN_PANEL_DOCUMENTATION.md`

