# 📝 Создание файла supabase/.env для локального Supabase

## ⚡ Быстрая инструкция

### Шаг 1: Создайте файл

В папке `supabase/` создайте файл с именем `.env` (с точкой в начале!)

### Шаг 2: Скопируйте содержимое

Скопируйте следующее содержимое в файл `supabase/.env`:

```env
# Отключение подтверждения email
GOTRUE_MAILER_AUTOCONFIRM=true

# URL вашего локального сайта
GOTRUE_SITE_URL=http://localhost:8080
```

### Шаг 3: Сохраните файл

Убедитесь, что файл сохранен как `supabase/.env`

### Шаг 4: Перезапустите Supabase

После создания файла перезапустите Supabase:

**Если используете Docker Compose:**
```bash
docker-compose restart auth
```

**Если используете Supabase CLI:**
```bash
supabase stop
supabase start
```

## 📋 Полное содержимое файла

```env
# ============================================
# Конфигурация для локального Self-Hosted Supabase
# ============================================

# Отключение подтверждения email
# true = пользователь сразу авторизуется без подтверждения
GOTRUE_MAILER_AUTOCONFIRM=true

# URL вашего локального сайта
GOTRUE_SITE_URL=http://localhost:8080
```

## ✅ Проверка

После создания файла и перезапуска:

1. Попробуйте зарегистрировать нового пользователя
2. Пользователь должен **сразу** авторизоваться без подтверждения email

## 🔧 Если используете Docker Compose

Убедитесь, что в `docker-compose.yml` файл `.env` подключен к сервису `auth`:

```yaml
services:
  auth:
    env_file:
      - ./supabase/.env
    # или напрямую:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "true"
      GOTRUE_SITE_URL: "http://localhost:8080"
```

## 📚 Дополнительная информация

- Подробная инструкция: `LOCAL_SUPABASE_SETUP.md`
- Пример файла: `supabase.env.example`








