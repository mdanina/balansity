# 🚀 Настройка Supabase на Beget

## 📋 Шаг 1: Вход в админ-панель Supabase

### Вариант 1: Через домен
1. Откройте браузер
2. Перейдите по адресу: `https://oyuyienekon.beget.app`
3. Войдите используя:
   - **Имя пользователя:** `user`
   - **Пароль:** `G3%DvQmA`

### Вариант 2: Через панель управления Beget
1. Войдите в панель управления Beget
2. Найдите установленное приложение Supabase
3. Откройте админ-панель

---

## 🔑 Шаг 2: Получение API ключей

После входа в админ-панель Supabase:

1. **Найдите раздел Settings (Настройки)**
   - Обычно в левом меню или в верхней панели
   - Или перейдите по адресу: `https://oyuyienekon.beget.app/project/default/settings/api`

2. **Найдите раздел "API" или "Project API keys"**

3. **Скопируйте следующие данные:**
   - **Project URL** - обычно это `https://oyuyienekon.beget.app` или `https://oyuyienekon.beget.app:8000`
   - **anon public key** - публичный ключ (начинается с `eyJ...`)
   - ⚠️ **НЕ копируйте** `service_role` ключ!

---

## 🔧 Шаг 3: Определение правильного URL

Для self-hosted Supabase URL может быть:
- `https://oyuyienekon.beget.app` (основной домен)
- `https://oyuyienekon.beget.app:8000` (с портом для API)
- `http://oyuyienekon.beget.app:8000` (HTTP вместо HTTPS)

**Как проверить:**
1. В админ-панели Supabase найдите раздел Settings → API
2. Там должен быть указан **Project URL**
3. Используйте именно этот URL

---

## 📝 Шаг 4: Создание файла .env.local

В корне вашего проекта создайте файл `.env.local`:

```env
VITE_SUPABASE_URL=https://oyuyienekon.beget.app
VITE_SUPABASE_ANON_KEY=ваш-anon-key-здесь
```

**Замените:**
- URL на тот, который указан в админ-панели
- `ваш-anon-key-здесь` на скопированный anon public key

---

## 🗄️ Шаг 5: Применение SQL миграций

1. В админ-панели Supabase найдите **SQL Editor** (Редактор SQL)
2. Выполните миграции по порядку:

### Миграция 1: Создание таблиц
1. Откройте файл `supabase/migrations/001_initial_schema.sql` в вашем проекте
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run** или **Выполнить**
5. Дождитесь сообщения об успехе

### Миграция 2: Безопасность (RLS)
1. Откройте `supabase/migrations/002_rls_policies.sql`
2. Скопируйте и выполните SQL код

### Миграция 3: Функции
1. Откройте `supabase/migrations/003_scoring_functions.sql`
2. Скопируйте и выполните SQL код

---

## ✅ Шаг 6: Проверка

### Проверка таблиц:
1. В админ-панели найдите **Table Editor**
2. Должны быть видны таблицы:
   - `users`
   - `profiles`
   - `assessments`
   - `answers`

### Проверка подключения:
После настройки `.env.local` перезапустите dev сервер:
```bash
npm run dev
```

Откройте консоль браузера (F12) - не должно быть ошибок подключения к Supabase.

---

## ⚠️ Возможные проблемы

### Проблема: Не могу войти в админ-панель
**Решение:**
- Проверьте правильность URL
- Попробуйте через панель управления Beget
- Убедитесь, что Supabase запущен на сервере

### Проблема: Не могу найти API ключи
**Решение:**
- В self-hosted Supabase ключи могут быть в другом месте
- Попробуйте найти раздел "Project Settings" или "API Settings"
- Или проверьте файлы конфигурации на сервере

### Проблема: CORS ошибки
**Решение:**
- Нужно настроить CORS в Supabase
- Добавьте ваш домен в список разрешенных источников
- Обычно это делается в настройках проекта

### Проблема: Неправильный URL
**Решение:**
- Проверьте, какой порт использует Supabase API
- Обычно это порт 8000
- Попробуйте: `https://oyuyienekon.beget.app:8000`

---

## 📞 Что делать дальше

После получения URL и ключа:
1. Создайте `.env.local` с этими данными
2. Примените SQL миграции
3. Установите зависимость: `npm install @supabase/supabase-js`
4. Перезапустите dev сервер

---

## 🔐 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env.local` в Git
- Не делитесь ключами публично
- Используйте только `anon` ключ на фронтенде
- `service_role` ключ - только для серверных операций

---

Если возникнут проблемы - напишите, помогу разобраться! 🚀






