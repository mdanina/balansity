# 🏠 Настройка локального Self-Hosted Supabase

## ✅ Что уже сделано

Создан файл `supabase/.env` с настройками для отключения подтверждения email.

## 🔧 Что нужно сделать дальше

### Вариант 1: Если используете Docker Compose

1. Найдите файл `docker-compose.yml` (обычно в папке с Supabase или в корне проекта)
2. Убедитесь, что файл `supabase/.env` подключен к сервису `auth`:

```yaml
services:
  auth:
    env_file:
      - ./supabase/.env
    # или напрямую в environment:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "true"
      GOTRUE_SITE_URL: "http://localhost:8080"
```

3. Перезапустите сервис:
```bash
docker-compose restart auth
# или
docker-compose down && docker-compose up -d
```

### Вариант 2: Если используете Supabase CLI

1. Убедитесь, что файл `supabase/.env` находится в правильной папке
2. Перезапустите Supabase:
```bash
supabase stop
supabase start
```

### Вариант 3: Если Supabase запущен вручную

1. Скопируйте переменные из `supabase/.env` в вашу конфигурацию
2. Перезапустите сервис auth

## 📋 Содержимое файла supabase/.env

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=http://localhost:8080
```

## ✅ Проверка

После настройки:

1. Перезапустите Supabase (см. варианты выше)
2. Попробуйте зарегистрировать нового пользователя в приложении
3. Пользователь должен **сразу** авторизоваться без подтверждения email
4. Не должно быть сообщений о необходимости подтвердить email

## 🔍 Как проверить, что настройки применились

### Через Docker:

```bash
# Проверьте переменные окружения контейнера auth
docker exec <container_name> env | grep GOTRUE

# Или через docker-compose
docker-compose exec auth env | grep GOTRUE
```

Должны увидеть:
```
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=http://localhost:8080
```

## 🚨 Если не работает

1. **Убедитесь, что файл `.env` подключен:**
   - Проверьте `docker-compose.yml` на наличие `env_file: - ./supabase/.env`
   - Или проверьте, что переменные указаны в `environment:`

2. **Перезапустите сервис:**
   - После изменения `.env` обязательно перезапустите auth сервис

3. **Проверьте логи:**
   ```bash
   docker-compose logs auth
   # или
   docker logs <container_name>
   ```

4. **Убедитесь, что используется правильный файл:**
   - Файл должен быть в `supabase/.env`
   - Или путь должен быть указан правильно в docker-compose.yml

## 📚 Дополнительная информация

- Пример файла: `supabase.env.example`
- Подробная инструкция: `DISABLE_EMAIL_CONFIRMATION.md`

