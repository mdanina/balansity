# 🚫 Отключение подтверждения email для Self-Hosted Supabase

Эта инструкция поможет отключить подтверждение email в вашем self-hosted Supabase.

## ⚡ Быстрая настройка

### Шаг 1: Найдите конфигурационные файлы

На вашем сервере найдите один из файлов:
- `docker-compose.yml` (если используете Docker Compose)
- `.env` (файл с переменными окружения)
- Настройки в панели управления (Beget, Docker и т.д.)

### Шаг 2: Добавьте/измените переменную окружения

Добавьте или измените следующую переменную:

```env
GOTRUE_MAILER_AUTOCONFIRM=true
```

**Важно:** `true` = без подтверждения email (пользователь сразу авторизуется)

### Шаг 3: Перезапустите сервис auth

После изменения переменных окружения перезапустите сервис:

```bash
# Для Docker Compose
docker-compose restart auth

# Или полный перезапуск
docker-compose down && docker-compose up -d
```

## 📋 Примеры конфигурации

### Docker Compose (docker-compose.yml)

```yaml
services:
  auth:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "true"  # Отключить подтверждение
      GOTRUE_SITE_URL: "https://ваш-домен.com"  # Ваш домен
```

### .env файл

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=https://ваш-домен.com
```

### Панель управления Beget

1. Войдите в панель управления Beget
2. Найдите ваше приложение Supabase
3. Перейдите в "Environment Variables" или "Переменные окружения"
4. Добавьте:
   - **Ключ:** `GOTRUE_MAILER_AUTOCONFIRM`
   - **Значение:** `true`
5. Сохраните и перезапустите приложение

## ✅ Проверка

После настройки:

1. Попробуйте зарегистрировать нового пользователя
2. Пользователь должен **сразу** авторизоваться без подтверждения email
3. Не должно быть сообщений о необходимости подтвердить email

## 🔄 Если нужно включить обратно

Чтобы включить подтверждение email обратно, установите:

```env
GOTRUE_MAILER_AUTOCONFIRM=false
```

И настройте SMTP (см. `SUPABASE_SELF_HOSTED_EMAIL_SETUP.md`).

## 📚 Дополнительная информация

- Подробная инструкция: `SUPABASE_SELF_HOSTED_EMAIL_SETUP.md`
- Миграция SQL: `supabase/migrations/005_disable_email_confirmation.sql`

## ⚠️ Важно

- После изменения переменных окружения **обязательно** перезапустите сервис `auth`
- Убедитесь, что `GOTRUE_SITE_URL` указывает на правильный домен
- Для продакшена рекомендуется включить подтверждение email (`GOTRUE_MAILER_AUTOCONFIRM=false`) и настроить SMTP










