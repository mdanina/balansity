# 📧 Настройка SMTP для авторизации пользователей

## ✅ Что было настроено

Настроен SMTP сервер для отправки писем авторизации (подтверждение регистрации, сброс пароля) через почтовый сервер хостинга nic.ru.

### 📋 Параметры подключения

- **Домен:** `balansity.ru`
- **SMTP сервер:** `mail.nic.ru`
- **Порт:** `587` (TLS)
- **Email отправителя:** `noreply@balansity.ru`
- **Пароль:** `ILoveBalansity100!`
- **Имя отправителя:** `Little Otter`

---

## 🔧 Настройка в файле .env

### Где находится файл

Файл `.env` находится на сервере, где установлен Supabase:
- Обычно: `/opt/supabase/.env` или `/home/user/supabase/.env`
- Или рядом с `docker-compose.yml`

### Что было изменено

В секции `############ # Auth - Configuration for the GoTrue authentication server. ############` были обновлены следующие параметры:

#### ❌ Было (старые значения):
```env
## Email auth
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=true
GOTRUE_MAILER_AUTOCONFIRM=true
SMTP_ADMIN_EMAIL=admin@example.com
SMTP_HOST=supabase-mail
SMTP_PORT=2500
SMTP_USER=fake_mail_user
SMTP_PASS=fake_mail_password
SMTP_SENDER_NAME=fake_sender
ENABLE_ANONYMOUS_USERS=false
```

#### ✅ Стало (новые значения):
```env
## Email auth
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=false
GOTRUE_MAILER_AUTOCONFIRM=false
SMTP_ADMIN_EMAIL=noreply@balansity.ru
SMTP_HOST=mail.nic.ru
SMTP_PORT=587
SMTP_USER=noreply@balansity.ru
SMTP_PASS=ILoveBalansity100!
SMTP_SENDER_NAME=Little Otter
ENABLE_ANONYMOUS_USERS=false
```

### 📝 Полная секция Auth после изменений

```env
############
# Auth - Configuration for the GoTrue authentication server.
############

## General
SITE_URL=https://oyuyienekon.beget.app
ADDITIONAL_REDIRECT_URLS=
JWT_EXPIRY=3600
DISABLE_SIGNUP=false
API_EXTERNAL_URL=https://oyuyienekon.beget.app

## Mailer Config
MAILER_URLPATHS_CONFIRMATION="/auth/v1/verify"
MAILER_URLPATHS_INVITE="/auth/v1/verify"
MAILER_URLPATHS_RECOVERY="/auth/v1/verify"
MAILER_URLPATHS_EMAIL_CHANGE="/auth/v1/verify"

## Email auth
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=false
GOTRUE_MAILER_AUTOCONFIRM=false
SMTP_ADMIN_EMAIL=noreply@balansity.ru
SMTP_HOST=mail.nic.ru
SMTP_PORT=587
SMTP_USER=noreply@balansity.ru
SMTP_PASS=ILoveBalansity100!
SMTP_SENDER_NAME=Little Otter
ENABLE_ANONYMOUS_USERS=false

## Phone auth
ENABLE_PHONE_SIGNUP=true
ENABLE_PHONE_AUTOCONFIRM=true
```

---

## ⚠️ ВАЖНО: Перезапуск контейнера

**После изменения файла `.env` ОБЯЗАТЕЛЬНО нужно перезапустить контейнер `auth`, иначе изменения не вступят в силу!**

### Способы перезапуска

#### Способ 1: Через SSH (если есть доступ)

```bash
# 1. Подключитесь к серверу
ssh user@oyuyienekon.beget.app

# 2. Перейдите в папку с Supabase
cd ~/supabase || cd /opt/supabase

# 3. Перезапустите контейнер auth
docker-compose restart auth

# Или если docker-compose не найден:
docker ps | grep auth
docker restart <имя_контейнера_auth>
```

#### Способ 2: Через панель управления Beget

1. Войдите в панель управления Beget: https://cp.beget.com/
2. Найдите раздел "Домены" или "Приложения"
3. Найдите установленное приложение Supabase
4. Откройте настройки приложения
5. Найдите раздел "Docker" или "Контейнеры"
6. Найдите контейнер `auth` и нажмите "Перезапустить"

#### Способ 3: Через файловый менеджер Beget

1. Войдите в панель Beget
2. Откройте "Файловый менеджер"
3. Найдите папку с Supabase
4. Рядом с `docker-compose.yml` может быть кнопка "Управление контейнерами"

#### Способ 4: Обратиться в поддержку Beget

Если нет доступа к перезапуску:
- Обратитесь в поддержку Beget
- Попросите перезапустить контейнер `auth` в Supabase
- Укажите, что изменили настройки SMTP в `.env`

---

## 🔍 Проверка настроек

### Проверка 1: Убедиться, что файл .env изменен

```bash
# На сервере проверьте содержимое файла
cat /opt/supabase/.env | grep SMTP
# или
cat ~/supabase/.env | grep SMTP
```

Должны быть видны новые значения:
- `SMTP_HOST=mail.nic.ru`
- `SMTP_PORT=587`
- `SMTP_USER=noreply@balansity.ru`

### Проверка 2: Проверить переменные окружения контейнера

```bash
# После перезапуска проверьте переменные
docker-compose exec auth env | grep SMTP
# или
docker exec <container_name> env | grep SMTP
```

### Проверка 3: Проверить логи контейнера

```bash
# Проверьте логи на наличие ошибок SMTP
docker-compose logs auth | grep -i smtp
# или
docker logs <container_name> | grep -i smtp
```

Не должно быть ошибок подключения к SMTP серверу.

---

## ✅ Тестирование работы

### Шаг 1: Зарегистрировать тестового пользователя

1. Откройте приложение
2. Зарегистрируйте нового пользователя с реальным email
3. Нажмите "Зарегистрироваться"

### Шаг 2: Проверить почту

1. Откройте почтовый ящик, указанный при регистрации
2. Проверьте папку "Входящие" и "Спам"
3. Должно прийти письмо с темой типа "Подтвердите регистрацию" или "Confirm your email"

### Шаг 3: Подтвердить email

1. Нажмите на ссылку в письме
2. Должно произойти перенаправление на страницу подтверждения
3. Email должен быть подтвержден
4. Пользователь сможет войти в систему

---

## 🚨 Решение проблем

### Проблема: Письма не приходят

**Возможные причины и решения:**

1. **Контейнер не перезапущен**
   - ✅ Решение: Перезапустите контейнер `auth` (см. раздел "Перезапуск контейнера")

2. **Неправильные настройки SMTP**
   - ✅ Решение: Проверьте, что в `.env` указаны правильные значения
   - Проверьте: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`

3. **Почтовый ящик не создан**
   - ✅ Решение: Убедитесь, что `noreply@balansity.ru` создан в панели управления хостингом

4. **Порт заблокирован**
   - ✅ Решение: Попробуйте порт `465` (SSL) вместо `587`:
     ```env
     SMTP_PORT=465
     ```

5. **Письма попадают в спам**
   - ✅ Решение: Проверьте папку "Спам" в почтовом ящике
   - Настройте SPF/DKIM записи для домена (если возможно)

### Проблема: Ошибка подключения к SMTP

**Проверьте логи:**
```bash
docker-compose logs auth | tail -50
```

**Возможные ошибки:**
- `connection refused` - порт заблокирован или неправильный хост
- `authentication failed` - неправильный логин или пароль
- `certificate error` - проблема с SSL/TLS

**Решения:**
- Проверьте правильность `SMTP_HOST` и `SMTP_PORT`
- Убедитесь, что пароль скопирован правильно (без лишних пробелов)
- Попробуйте порт `465` с SSL вместо `587` с TLS

### Проблема: Изменения не применяются

**Причины:**
1. Контейнер не перезапущен после изменения `.env`
2. Файл `.env` не подключен к контейнеру
3. Используется `docker-compose.yml` вместо `.env`

**Решения:**
1. ✅ Обязательно перезапустите контейнер после изменений
2. Проверьте, что `.env` файл находится в правильной папке
3. Если используете `docker-compose.yml`, обновите переменные там

---

## 📋 Чек-лист настройки

- [ ] Почтовый ящик `noreply@balansity.ru` создан на хостинге
- [ ] Файл `.env` на сервере обновлен с новыми значениями SMTP
- [ ] Значение `GOTRUE_MAILER_AUTOCONFIRM=false` (включено подтверждение)
- [ ] Значение `ENABLE_EMAIL_AUTOCONFIRM=false` (отключено авто-подтверждение)
- [ ] Контейнер `auth` перезапущен после изменений
- [ ] Проверены логи контейнера (нет ошибок SMTP)
- [ ] Протестирована регистрация нового пользователя
- [ ] Письмо с подтверждением приходит на почту
- [ ] Ссылка в письме работает и подтверждает email

---

## 🔐 Безопасность

⚠️ **Важно:**
- Файл `.env` содержит пароли в открытом виде
- Не коммитьте файл `.env` в Git
- Ограничьте доступ к файлу на сервере:
  ```bash
  chmod 600 /opt/supabase/.env
  ```
- Регулярно меняйте пароли от почтовых ящиков

---

## 📚 Дополнительная информация

- **Документация Supabase:** https://supabase.com/docs/guides/auth
- **Документация GoTrue:** https://github.com/supabase/gotrue
- **SMTP настройки nic.ru:** См. инструкции в панели управления хостингом

---

## 📝 История изменений

- **Дата:** 2025-01-XX
- **Изменения:** Настроен SMTP через `mail.nic.ru` для домена `balansity.ru`
- **Email отправителя:** `noreply@balansity.ru`
- **Статус:** ✅ Настроено, требуется перезапуск контейнера

