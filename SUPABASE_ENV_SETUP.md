# 🔧 Настройка .env файла для Self-Hosted Supabase

## ⚠️ Важно

Файл `.env` для Supabase должен находиться **на сервере**, где запущен Supabase, а не в этом фронтенд-проекте!

## 📍 Где найти файл .env на сервере

Файл `.env` обычно находится:
- В папке с Supabase: `/opt/supabase/.env`
- В папке с docker-compose: `/home/user/supabase/.env`
- Рядом с `docker-compose.yml`

## 🚀 Быстрая настройка

### Шаг 1: Подключитесь к серверу

```bash
ssh user@ваш-сервер.com
```

### Шаг 2: Найдите файл .env

```bash
# Поиск файла .env
find / -name ".env" -path "*/supabase/*" 2>/dev/null

# Или проверьте стандартные места:
ls -la /opt/supabase/.env
ls -la ~/supabase/.env
```

### Шаг 3: Отредактируйте файл .env

```bash
# Откройте файл в редакторе
nano /opt/supabase/.env
# или
vi /opt/supabase/.env
```

### Шаг 4: Добавьте переменные

Добавьте следующие строки в файл `.env`:

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=https://ваш-домен.com
```

**Важно:** Замените `https://ваш-домен.com` на реальный URL вашего сайта!

### Шаг 5: Сохраните и перезапустите

```bash
# Сохраните файл (в nano: Ctrl+O, Enter, Ctrl+X)
# В vi: :wq

# Перезапустите сервис auth
docker-compose restart auth

# Или если используете docker напрямую:
docker restart supabase_auth_1
```

## 📋 Полный пример .env файла

См. файл `supabase.env.example` в этом проекте - там полный пример конфигурации.

## ✅ Проверка

После настройки:

1. Попробуйте зарегистрировать нового пользователя
2. Пользователь должен **сразу** авторизоваться без подтверждения email
3. Не должно быть сообщений о необходимости подтвердить email

## 🔍 Если не можете найти файл .env

Если файл `.env` не существует, создайте его:

```bash
# Создайте файл
nano /opt/supabase/.env

# Добавьте переменные (см. выше)
# Сохраните и перезапустите сервисы
```

## 📚 Дополнительная информация

- Подробная инструкция: `DISABLE_EMAIL_CONFIRMATION.md`
- Пример файла: `supabase.env.example`





