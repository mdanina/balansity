# Настройка Supabase для Little Otter / Balansity

## 📋 Шаги по настройке

### 1. Создание проекта в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Запишите:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **Anon Key** (публичный ключ)

### 2. Применение миграций

В Supabase Dashboard перейдите в **SQL Editor** и выполните миграции в следующем порядке:

1. **001_initial_schema.sql** - Создание таблиц
2. **002_rls_policies.sql** - Настройка безопасности (RLS)
3. **003_scoring_functions.sql** - Функции для подсчета баллов

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
```

⚠️ **Важно:** Не коммитьте `.env.local` в Git! Добавьте его в `.gitignore`.

### 4. Установка зависимостей

```bash
npm install
```

### 5. Проверка подключения

После настройки проверьте подключение, открыв консоль браузера - не должно быть ошибок подключения к Supabase.

---

## 🗄️ Структура базы данных

### Таблицы

1. **users** - Родительские аккаунты (расширение auth.users)
2. **profiles** - Члены семьи (родитель + дети)
3. **assessments** - Сессии диагностики
4. **answers** - Ответы на вопросы

### Функции

- `get_active_assessment(profile_id, assessment_type)` - Получить/создать активную оценку
- `complete_assessment(assessment_id)` - Завершить оценку и рассчитать результаты
- `calculate_checkup_scores(assessment_id)` - Подсчет баллов для Checkup

---

## 🔒 Безопасность (RLS)

Все таблицы защищены Row Level Security (RLS):
- Пользователь видит только свои данные
- Пользователь может редактировать только свои профили и оценки
- Автоматическая проверка через `auth.uid()`

---

## 💰 Paywall (Монетизация)

Поле `is_paid` в таблице `assessments`:
- `false` - показывать только базовые результаты (с блюром)
- `true` - показывать полный отчет

При оплате через ЮКассу/другую систему вебхук обновляет это поле.

---

## 📊 Логика работы

### Сохранение ответов

```typescript
// При ответе на вопрос
await saveAnswer(assessmentId, {
  questionId: 1,
  questionCode: 'checkup_01',
  category: 'О ребенке',
  value: 2,
  answerType: 'default',
  stepNumber: 1
});

// Обновление прогресса
await updateAssessmentStep(assessmentId, 2);
```

### Восстановление прогресса

```typescript
// При загрузке страницы
const assessment = await getActiveAssessment(profileId, 'checkup');
if (assessment) {
  // Продолжить с assessment.current_step
}
```

### Завершение оценки

```typescript
// После последнего вопроса
const results = await completeAssessment(assessmentId);
// results содержит рассчитанные баллы и статусы
```

---

## 🧪 Тестирование

После настройки проверьте:

1. ✅ Создание профиля
2. ✅ Создание оценки
3. ✅ Сохранение ответов
4. ✅ Восстановление прогресса
5. ✅ Завершение оценки и расчет результатов

---

## 📝 Примечания

- Все даты хранятся в формате ISO 8601 (timestamptz)
- JSONB используется для гибкого хранения результатов
- Уникальность ответов: один ответ на один вопрос в одной сессии
- Автоматическое обновление `updated_at` через триггеры







