# ✅ Проверка настройки Supabase

## 🔍 Шаг 1: Проверка таблиц

1. В админ-панели Supabase найдите **Table Editor** (Редактор таблиц)
   - Обычно в левом меню или по адресу: `https://oyuyienekon.beget.app/project/default/editor`

2. Должны быть видны таблицы:
   - ✅ `users` - пользователи
   - ✅ `profiles` - члены семьи
   - ✅ `assessments` - сессии диагностики
   - ✅ `answers` - ответы на вопросы

---

## 🔍 Шаг 2: Проверка функций

1. В SQL Editor выполните запрос:
```sql
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'public' 
AND routine_type = 'FUNCTION'
ORDER BY routine_name;
```

2. Должны быть видны функции:
   - ✅ `calculate_checkup_scores`
   - ✅ `complete_assessment`
   - ✅ `get_active_assessment`
   - ✅ `handle_new_user`
   - ✅ `update_updated_at_column`

---

## 🔍 Шаг 3: Проверка RLS (безопасности)

1. В Table Editor выберите любую таблицу (например, `profiles`)
2. Перейдите на вкладку **Policies** (Политики)
3. Должны быть видны политики безопасности для каждой таблицы

---

## 🚀 Шаг 4: Проверка подключения в приложении

1. **Перезапустите dev сервер:**
   ```bash
   npm run dev
   ```

2. **Откройте приложение в браузере**

3. **Откройте консоль браузера** (F12 → Console)

4. **Проверьте:**
   - ✅ Нет ошибок типа "Supabase URL not found"
   - ✅ Нет ошибок подключения
   - ✅ Нет CORS ошибок

---

## ⚠️ Если есть ошибки

### Ошибка "Supabase URL not found":
- Проверьте, что файл `.env.local` создан правильно
- Убедитесь, что перезапустили dev сервер после создания `.env.local`

### CORS ошибки:
- Может потребоваться настройка CORS в Supabase
- Обычно это делается в настройках проекта

### Ошибки подключения:
- Проверьте правильность URL в `.env.local`
- Проверьте правильность anon key
- Убедитесь, что Supabase запущен на сервере

---

## 🎉 Готово!

Если все проверки пройдены:
- ✅ База данных настроена
- ✅ Таблицы созданы
- ✅ Безопасность настроена
- ✅ Функции созданы
- ✅ Приложение подключено

Теперь можно тестировать приложение! 🚀











