# 🔑 Настройка восстановления пароля для Self-Hosted Supabase

В **self-hosted Supabase** нет веб-интерфейса как в cloud версии. Все настройки делаются через **переменные окружения GoTrue**.

## 📍 Где найти настройки

### Вариант 1: Файл `.env` на сервере

Обычно находится в папке с Supabase, например:
- `/opt/beget/supabase/.env`
- `/opt/supabase/.env`
- `~/supabase/.env`

### Вариант 2: Файл `docker-compose.yml` на сервере

Обычно находится в той же папке, что и `.env`:
- `/opt/beget/supabase/docker-compose.yml`
- `/opt/supabase/docker-compose.yml`

## 🔧 Настройка Redirect URLs для восстановления пароля

Для работы восстановления пароля нужно настроить следующие переменные окружения:

### Способ 1: Через `.env` файл

Откройте файл `.env` на сервере и добавьте/измените:

```env
# Основной URL вашего сайта
GOTRUE_SITE_URL=https://ваш-домен.com

# Путь для ссылки восстановления пароля в письме
# Важно: должно совпадать с роутом в вашем приложении
MAILER_URLPATHS_RECOVERY=/reset-password
# Или с префиксом GOTRUE_ (новый формат):
# GOTRUE_MAILER_URLPATHS_RECOVERY=/reset-password

# Список разрешенных URL для редиректов (опционально, но рекомендуется)
# Разделяйте несколько URL запятыми
GOTRUE_URI_ALLOW_LIST=https://ваш-домен.com/reset-password,https://ваш-домен.com
```

**Для разработки:**
```env
GOTRUE_SITE_URL=http://localhost:8080
MAILER_URLPATHS_RECOVERY=/reset-password
GOTRUE_URI_ALLOW_LIST=http://localhost:8080/reset-password,http://localhost:8080
```

**Для продакшена:**
```env
GOTRUE_SITE_URL=https://ваш-домен.com
MAILER_URLPATHS_RECOVERY=/reset-password
GOTRUE_URI_ALLOW_LIST=https://ваш-домен.com/reset-password,https://ваш-домен.com
```

### Способ 2: Через `docker-compose.yml`

Найдите секцию `auth` (или `gotrue`) и добавьте в `environment`:

```yaml
services:
  auth:
    environment:
      # Основной URL
      GOTRUE_SITE_URL: "https://ваш-домен.com"
      
      # Путь для ссылки восстановления пароля в письме
      MAILER_URLPATHS_RECOVERY: "/reset-password"
      # Или с префиксом GOTRUE_:
      # GOTRUE_MAILER_URLPATHS_RECOVERY: "/reset-password"
      
      # Список разрешенных URL для редиректов
      GOTRUE_URI_ALLOW_LIST: "https://ваш-домен.com/reset-password,https://ваш-домен.com"
```

**Для разработки:**
```yaml
services:
  auth:
    environment:
      GOTRUE_SITE_URL: "http://localhost:8080"
      GOTRUE_URI_ALLOW_LIST: "http://localhost:8080/reset-password,http://localhost:8080"
```

**Для продакшена:**
```yaml
services:
  auth:
    environment:
      GOTRUE_SITE_URL: "https://ваш-домен.com"
      GOTRUE_URI_ALLOW_LIST: "https://ваш-домен.com/reset-password,https://ваш-домен.com"
```

## 📋 Полный пример конфигурации с SMTP

Если у вас уже настроен SMTP для отправки писем:

**`.env` файл:**
```env
# Основные настройки
GOTRUE_SITE_URL=https://ваш-домен.com
MAILER_URLPATHS_RECOVERY=/reset-password
GOTRUE_URI_ALLOW_LIST=https://ваш-домен.com/reset-password,https://ваш-домен.com

# SMTP настройки (если нужны)
GOTRUE_SMTP_HOST=smtp.beget.com
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Balansity

# Подтверждение email (false = требуется подтверждение)
GOTRUE_MAILER_AUTOCONFIRM=false
```

**`docker-compose.yml`:**
```yaml
services:
  auth:
    environment:
      GOTRUE_SITE_URL: "https://ваш-домен.com"
      MAILER_URLPATHS_RECOVERY: "/reset-password"
      GOTRUE_URI_ALLOW_LIST: "https://ваш-домен.com/reset-password,https://ваш-домен.com"
      GOTRUE_SMTP_HOST: smtp.beget.com
      GOTRUE_SMTP_PORT: "587"
      GOTRUE_SMTP_USER: "ваш-email@beget.com"
      GOTRUE_SMTP_PASS: "ваш-пароль"
      GOTRUE_SMTP_ADMIN_EMAIL: "noreply@ваш-домен.com"
      GOTRUE_SMTP_SENDER_NAME: "Balansity"
      GOTRUE_MAILER_AUTOCONFIRM: "false"
```

## 🔄 Перезапуск сервиса

После изменения настроек **обязательно перезапустите сервис auth**:

```bash
# Если используете docker-compose
docker-compose restart auth

# Или полный перезапуск
docker-compose down && docker-compose up -d
```

## ✅ Проверка настроек

Чтобы проверить, что настройки применились:

```bash
# Посмотреть переменные окружения контейнера auth
docker-compose exec auth env | grep GOTRUE

# Или если знаете имя контейнера
docker exec <container_name> env | grep GOTRUE
```

Вы должны увидеть:
```
GOTRUE_SITE_URL=https://ваш-домен.com
GOTRUE_URI_ALLOW_LIST=https://ваш-домен.com/reset-password,https://ваш-домен.com
```

## 🔍 Как это работает

1. Когда пользователь запрашивает восстановление пароля на странице `/forgot-password`
2. Supabase отправляет письмо со ссылкой вида: `https://ваш-домен.com/reset-password#access_token=...&type=recovery`
3. Пользователь переходит по ссылке
4. Страница `/reset-password` извлекает токен из URL и устанавливает сессию восстановления
5. Пользователь вводит новый пароль

## ⚠️ Важные замечания

1. **`GOTRUE_SITE_URL`** — основной URL, который используется Supabase для формирования ссылок в письмах
2. **`MAILER_URLPATHS_RECOVERY`** или **`GOTRUE_MAILER_URLPATHS_RECOVERY`** — путь в ссылке восстановления пароля. По умолчанию `/auth/v1/verify`, но должен быть изменен на `/reset-password`, чтобы совпадать с вашим роутом
3. **`GOTRUE_URI_ALLOW_LIST`** (опционально) — список разрешенных URL для редиректов. Если не указан, Supabase будет использовать `GOTRUE_SITE_URL` и базовые пути
4. **`GOTRUE_MAILER_EXTERNAL_HOSTS`** (опционально) — список хостов, разрешенных в ссылках email. Это защита от открытых редиректов. Должен содержать ваш домен (без протокола). Например: `oyuyienekon.beget.app` или `oyuyienekon.beget.app,www.oyuyienekon.beget.app` если используете оба варианта
3. После изменения переменных **всегда перезапускайте сервис auth**, иначе изменения не применятся
4. Убедитесь, что в `GOTRUE_URI_ALLOW_LIST` указаны URL **без слеша в конце** (т.е. `https://domain.com/reset-password`, а не `https://domain.com/reset-password/`)

## 🚨 Решение проблем

### Письмо не приходит

1. Проверьте настройки SMTP:
   ```bash
   docker-compose exec auth env | grep GOTRUE_SMTP
   ```

2. Проверьте логи auth сервиса:
   ```bash
   docker-compose logs auth | tail -50
   ```

3. Убедитесь, что `GOTRUE_SITE_URL` указан правильно

### Ссылка в письме не работает

1. Убедитесь, что `GOTRUE_URI_ALLOW_LIST` содержит правильный URL:
   ```bash
   docker-compose exec auth env | grep GOTRUE_URI_ALLOW_LIST
   ```

2. Проверьте, что URL в списке точно совпадает с URL вашего приложения (протокол, домен, порт)

3. Перезапустите сервис auth после изменений

### Ошибка "Invalid redirect URL"

Это означает, что URL не находится в списке разрешенных. Убедитесь, что:
- `GOTRUE_URI_ALLOW_LIST` содержит нужный URL
- URL написан точно так же, как в браузере (с/без `www`, `http`/`https` и т.д.)
- Сервис auth был перезапущен после изменений

## 📚 Дополнительные ресурсы

- [GoTrue Configuration](https://github.com/supabase/gotrue#configuration) - полная документация по настройкам GoTrue
- [Self-Hosted Supabase Email Setup](./SUPABASE_SELF_HOSTED_EMAIL_SETUP.md) - общая настройка email

