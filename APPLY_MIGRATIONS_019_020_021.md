# 🗄️ Применение миграций 019, 020, 021: Консультации и оплата

## ⚠️ Важно: Применяйте миграции строго по порядку!

---

## 📍 Шаг 1: Откройте SQL Editor в Supabase

1. В админ-панели Supabase перейдите в **SQL Editor**
   - Обычно в меню слева или по адресу: `https://oyuyienekon.beget.app/project/default/sql`
   
2. Нажмите **"New query"** (Новый запрос)

---

## 📝 Шаг 2: Примените миграцию 019 (ИСПРАВЛЕННАЯ)

### ⚠️ Если таблицы уже частично созданы, сначала удалите их:

```sql
DROP TABLE IF EXISTS public.package_purchases CASCADE;
DROP TABLE IF EXISTS public.appointments CASCADE;
DROP TABLE IF EXISTS public.packages CASCADE;
DROP TABLE IF EXISTS public.appointment_types CASCADE;
DROP TABLE IF EXISTS public.payments CASCADE;
```

### Затем примените миграцию:

1. Откройте файл `supabase/migrations/019_appointments_and_payments.sql`
2. **Выделите весь текст** (Ctrl+A)
3. **Скопируйте** (Ctrl+C)
4. Вставьте в SQL Editor в Supabase (Ctrl+V)
5. Нажмите **Run** (Выполнить) или `Ctrl+Enter`
6. Дождитесь сообщения **"Success"**

---

## 📝 Шаг 3: Примените миграцию 020 (Начальные данные)

1. Откройте файл `supabase/migrations/020_initial_appointment_data.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь **"Success"**

Эта миграция добавляет:
- Тип консультации "Первичная встреча" (бесплатная, 30 минут)
- Тип консультации "Сессия для ребенка" (5000 руб, 45 минут)
- Тип консультации "Сессия для родителя" (5000 руб, 45 минут)
- Пакеты сессий (4/8/12 сессий для каждого типа)

---

## 📝 Шаг 4: Примените миграцию 021 (Отслеживание бесплатной консультации)

1. Откройте файл `supabase/migrations/021_add_free_consultation_tracking.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь **"Success"**

Эта миграция добавляет поле `free_consultation_created` в таблицу `users` для отслеживания, была ли создана бесплатная консультация.

---

## ✅ Шаг 5: Проверка

### Проверка таблиц:

1. В админ-панели найдите **Table Editor**
2. Должны быть видны новые таблицы:
   - ✅ `appointment_types` (3 записи)
   - ✅ `appointments`
   - ✅ `packages` (6 записей)
   - ✅ `package_purchases`
   - ✅ `payments`

### Проверка данных:

Выполните в SQL Editor:
```sql
-- Проверить типы консультаций
SELECT * FROM public.appointment_types ORDER BY price;

-- Проверить пакеты
SELECT p.*, at.name as appointment_type_name 
FROM public.packages p
JOIN public.appointment_types at ON p.appointment_type_id = at.id
ORDER BY at.name, p.session_count;
```

---

## 🎉 Готово!

После успешного применения всех миграций:
- ✅ Таблицы для консультаций и оплаты созданы
- ✅ Начальные данные добавлены
- ✅ Бесплатная консультация будет автоматически создаваться после первого чекапа
- ✅ Функционал консультаций готов к использованию

Теперь можно:
1. Открыть приложение
2. Перейти в Dashboard
3. Нажать "Получить консультацию"
4. Увидеть бесплатную консультацию (если уже прошел первый чекап) и платные консультации

---

## 📝 Чеклист

- [ ] Открыт SQL Editor в Supabase
- [ ] (Если нужно) Удалены старые таблицы
- [ ] Применена миграция 019 (исправленная)
- [ ] Применена миграция 020 (начальные данные)
- [ ] Применена миграция 021 (отслеживание бесплатной консультации)
- [ ] Проверены таблицы в Table Editor
- [ ] Проверены данные через SQL запросы
- [ ] Протестирован функционал в приложении

