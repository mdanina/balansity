# 🧪 Как проверить подключение к Supabase

## 📝 Что такое dev сервер?

**Dev сервер** - это локальный сервер для разработки, который запускает ваше приложение на вашем компьютере. Он нужен, чтобы видеть изменения в коде сразу в браузере.

---

## 🚀 Шаг 1: Запуск dev сервера

### Если сервер НЕ запущен:

1. **Откройте терминал** (командную строку) в папке вашего проекта
   - В VS Code: нажмите `Ctrl+`` (обратная кавычка) или меню Terminal → New Terminal
   - Или откройте PowerShell/CMD в папке проекта

2. **Выполните команду:**
   ```bash
   npm run dev
   ```

3. **Дождитесь сообщения** типа:
   ```
   VITE v5.x.x  ready in xxx ms
   
   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```

4. **Скопируйте адрес** (обычно `http://localhost:5173/`)

### Если сервер УЖЕ запущен:

- Просто оставьте его работать
- Или перезапустите (см. ниже)

---

## 🔄 Шаг 2: Перезапуск dev сервера (если нужно)

Если вы только что создали файл `.env.local`, нужно перезапустить сервер:

1. **В терминале, где запущен сервер:**
   - Нажмите `Ctrl+C` (это остановит сервер)

2. **Запустите снова:**
   ```bash
   npm run dev
   ```

3. **Дождитесь сообщения о готовности**

---

## 🌐 Шаг 3: Открыть приложение в браузере

1. **Откройте браузер** (Chrome, Firefox, Edge и т.д.)

2. **Введите адрес** из терминала (обычно `http://localhost:5173/`)

3. **Нажмите Enter**

4. **Должна открыться** ваша страница приложения

---

## 🔍 Шаг 4: Открыть консоль браузера

1. **На открытой странице приложения** нажмите клавишу **F12**
   - Или правой кнопкой мыши → "Проверить элемент" / "Inspect"
   - Или меню браузера → Инструменты разработчика

2. **Откроется панель разработчика** внизу или справа

3. **Перейдите на вкладку "Console"** (Консоль)
   - Обычно это вторая вкладка после "Elements"

---

## ✅ Шаг 5: Проверить ошибки

### Что искать в консоли:

**✅ Хорошо (нет ошибок):**
- Консоль пустая или показывает только обычные сообщения
- Нет красных ошибок

**❌ Плохо (есть ошибки):**
- Красные сообщения об ошибках
- Ошибки типа:
  - "Supabase URL not found"
  - "Failed to connect to Supabase"
  - "CORS error"
  - "Network error"

### Пример хорошей консоли:
```
(пусто или только обычные сообщения)
```

### Пример плохой консоли:
```
❌ Error: Supabase URL not found
❌ Failed to connect to Supabase
❌ CORS policy error
```

---

## 🎯 Что делать дальше?

### Если ошибок НЕТ:
✅ Всё работает! Приложение подключено к Supabase.

### Если ошибки ЕСТЬ:
1. **Скопируйте текст ошибки**
2. **Сообщите мне** - помогу исправить

---

## 📸 Визуальная подсказка

Когда откроете консоль (F12), вы увидите что-то вроде:

```
Console
─────────────────────────────
(пусто или обычные сообщения)
```

Или если есть ошибки:

```
Console
─────────────────────────────
❌ Error: Supabase URL not found
   at supabase.ts:5
```

---

## 💡 Советы

- **Консоль всегда внизу или справа** после нажатия F12
- **Красные сообщения = ошибки** (нужно исправить)
- **Желтые сообщения = предупреждения** (можно игнорировать)
- **Белые сообщения = информация** (обычно нормально)

---

Попробуйте выполнить эти шаги и напишите, что видите в консоли! 🚀










