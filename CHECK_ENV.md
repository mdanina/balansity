# Как проверить .env файл

## 📋 Быстрая проверка

### 1. Проверить существует ли файл

В корне проекта должен быть файл `.env` (точка в начале имени).

**Windows:**
```powershell
# В PowerShell
Test-Path .env

# Или через проводник - включите "Показывать скрытые файлы"
```

**Linux/Mac:**
```bash
ls -la | grep .env
```

---

### 2. Проверить содержимое файла

**Откройте файл `.env` в текстовом редакторе**

Он должен содержать:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш_ключ_здесь
```

---

### 3. Проверить формат

✅ **Правильно:**
```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

❌ **Неправильно:**
```env
# Без VITE_ префикса
SUPABASE_URL=https://...           # ❌ Не сработает в Vite

# С пробелами вокруг =
VITE_SUPABASE_URL = https://...    # ❌ Пробелы недопустимы

# В кавычках (не обязательно, но можно)
VITE_SUPABASE_URL="https://..."    # ✅ Работает, но кавычки не нужны
```

---

### 4. Где взять значения?

#### VITE_SUPABASE_URL:
1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите: **Project Settings** → **API**
4. Скопируйте **Project URL**

#### VITE_SUPABASE_ANON_KEY:
1. В том же разделе (Project Settings → API)
2. Скопируйте **anon/public** key (НЕ service_role!)

---

### 5. Автоматическая проверка

После запуска `npm run dev` приложение автоматически проверит переменные:

**Если все ОК:**
- Приложение запустится нормально
- Никаких ошибок в консоли

**Если переменные отсутствуют:**
- Приложение выбросит ошибку при запуске
- В консоли будет:
  ```
  Error: Критические переменные окружения не настроены:
  VITE_SUPABASE_URL не установлена
  VITE_SUPABASE_ANON_KEY не установлена
  ```

---

## 🛠️ Создать .env файл

Если файла нет, создайте его:

### Способ 1: Через редактор кода
1. В корне проекта создайте новый файл
2. Назовите его `.env` (с точкой в начале!)
3. Скопируйте содержимое из `.env.example`
4. Заполните реальные значения

### Способ 2: Через командную строку

**Windows PowerShell:**
```powershell
Copy-Item .env.example .env
# Затем откройте .env и заполните значения
```

**Linux/Mac:**
```bash
cp .env.example .env
# Затем откройте .env и заполните значения
```

---

## ✅ Чек-лист проверки

- [ ] Файл `.env` существует в корне проекта
- [ ] Файл содержит `VITE_SUPABASE_URL=...`
- [ ] Файл содержит `VITE_SUPABASE_ANON_KEY=...`
- [ ] URL начинается с `https://`
- [ ] URL заканчивается на `.supabase.co`
- [ ] Anon key начинается с `eyJ...` (JWT токен)
- [ ] Нет пробелов вокруг знака `=`
- [ ] Нет лишних кавычек (или правильное использование)
- [ ] При запуске `npm run dev` нет ошибок о переменных

---

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте `.env` в git!
- `.env` уже должен быть в `.gitignore`
- Не делитесь содержимым `.env` публично
- Anon key безопасно показывать (но лучше не нужно)
- **НИКОГДА** не используйте service_role key в фронтенде!

---

## 🐛 Проблемы

### Файл не читается
- Убедитесь, что файл называется именно `.env` (с точкой)
- Убедитесь, что файл в корне проекта (рядом с package.json)

### Переменные не загружаются
- Перезапустите dev сервер (`npm run dev`)
- В Vite переменные загружаются только при старте

### Ошибка "URL имеет неверный формат"
- Проверьте, что URL начинается с `https://`
- Убедитесь, что нет лишних символов




