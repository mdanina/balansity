# Настройка интеграций Supabase

## 📋 Обзор

После установки интеграций (Cron, Database Webhooks, Queues) были созданы миграции для автоматизации процессов в вашем сервисе Balansity.

## 🚀 Применение миграций

### Шаг 1: Применить миграции в Supabase

1. Откройте Supabase Dashboard → SQL Editor
2. Примените миграции по порядку:

```sql
-- 1. Настройка Cron jobs
-- Выполните содержимое файла: supabase/migrations/030_setup_cron_jobs.sql

-- 2. Настройка очередей
-- Выполните содержимое файла: supabase/migrations/031_setup_queues.sql

-- 3. Настройка триггеров (webhooks)
-- Выполните содержимое файла: supabase/migrations/032_webhook_triggers.sql
```

### Шаг 2: Проверка установки

#### Проверка Cron jobs:
```sql
SELECT * FROM cron.job 
WHERE jobname IN (
  'update-appointment-statuses', 
  'cleanup-old-assessments', 
  'send-appointment-reminders'
);
```

#### Проверка очередей:
```sql
SELECT * FROM pgmq.list_queues();
```

Должны быть очереди:
- `email_queue`
- `report_generation_queue`
- `payment_processing_queue`
- `analytics_queue`

#### Проверка триггеров:
```sql
SELECT trigger_name, event_object_table, event_manipulation 
FROM information_schema.triggers 
WHERE trigger_schema = 'public' 
  AND trigger_name IN (
    'on_checkup_completed', 
    'on_new_appointment', 
    'on_payment_status_change'
  );
```

## ✨ Что теперь работает автоматически

### 1. Автоматическое обновление статусов консультаций
- **Когда:** Каждые 5 минут
- **Что делает:** Обновляет статусы консультаций (`scheduled` → `in_progress` → `completed`)
- **Раньше:** Только при заходе на Dashboard
- **Теперь:** Всегда автоматически

### 2. Автоматическая обработка завершенных чекапов
- **Когда:** При завершении чекапа (статус меняется на `completed`)
- **Что делает:**
  - Добавляет задачу генерации PDF-отчета в очередь
  - Отправляет email уведомление пользователю
  - Обновляет аналитику
- **Раньше:** Ничего не происходило автоматически
- **Теперь:** Все происходит автоматически в фоне

### 3. Автоматические уведомления о записях
- **Когда:** При создании новой записи на консультацию
- **Что делает:** Отправляет email подтверждение
- **Раньше:** Нет уведомлений
- **Теперь:** Автоматическое подтверждение

### 4. Автоматические напоминания
- **Когда:** За 24 часа и за 1 час до консультации
- **Что делает:** Отправляет email напоминание
- **Раньше:** Нет напоминаний
- **Теперь:** Автоматические напоминания каждый час

### 5. Автоматическая обработка платежей
- **Когда:** При изменении статуса платежа
- **Что делает:**
  - Отправляет подтверждение об оплате
  - Обновляет статус консультации при успешной оплате
  - Уведомляет об ошибках оплаты
- **Раньше:** Только обновление статуса в БД
- **Теперь:** Полная автоматическая обработка

## 📝 Использование в коде

### Добавление задач в очереди

```typescript
import { queueEmailTask, queueReportGeneration } from '@/lib/queues';

// Отправка email
await queueEmailTask({
  to: 'user@example.com',
  subject: 'Ваш отчет готов',
  template: 'report_ready',
  variables: { name: 'Иван' }
});

// Генерация отчета
await queueReportGeneration(assessmentId, userId, 'pdf');
```

### Чтение задач из очередей (для обработчиков)

```typescript
import { readEmailTask, archiveTask } from '@/lib/queues';

// Получить следующую задачу
const task = await readEmailTask(30); // timeout 30 секунд

if (task) {
  // Обработать задачу
  await sendEmail(task.msg);
  
  // Отметить как выполненную
  await archiveTask('email_queue', task.msg_id);
}
```

## 🔧 Настройка обработчиков очередей

Для обработки задач из очередей нужно создать обработчики (workers). Это можно сделать через:

1. **Supabase Edge Functions** (рекомендуется)
2. **Отдельный сервис/worker**
3. **Периодический вызов из приложения**

Пример Edge Function для обработки email очереди:

```typescript
// supabase/functions/process-email-queue/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

serve(async (req) => {
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? ''
  )

  // Читаем задачу из очереди
  const { data: task } = await supabase.rpc('pgmq_read', {
    queue_name: 'email_queue',
    vt: 30
  })

  if (task) {
    // Отправляем email
    await sendEmail(task.msg)
    
    // Архивируем задачу
    await supabase.rpc('pgmq_archive', {
      queue_name: 'email_queue',
      msg_id: task.msg_id
    })
  }

  return new Response(JSON.stringify({ success: true }), {
    headers: { 'Content-Type': 'application/json' }
  })
})
```

## 📊 Мониторинг

### Проверка статуса Cron jobs:
```sql
SELECT * FROM cron.job_run_details 
ORDER BY start_time DESC 
LIMIT 10;
```

### Проверка статистики очередей:
```sql
SELECT * FROM pgmq_metrics('email_queue');
SELECT * FROM pgmq_metrics('report_generation_queue');
```

### Просмотр задач в очередях:
```sql
-- Email очередь
SELECT * FROM pgmq.email_queue ORDER BY enqueued_at DESC LIMIT 10;

-- Очередь отчетов
SELECT * FROM pgmq.report_generation_queue ORDER BY enqueued_at DESC LIMIT 10;
```

## ⚠️ Важные замечания

1. **Cron jobs** работают автоматически после применения миграции
2. **Триггеры** срабатывают автоматически при изменениях в БД
3. **Очереди** требуют обработчиков (workers) для выполнения задач
4. Для отправки email нужно настроить SMTP или использовать внешний сервис

## 🐛 Устранение неполадок

### Cron jobs не выполняются:
```sql
-- Проверьте, что расширение включено
SELECT * FROM pg_extension WHERE extname = 'pg_cron';

-- Проверьте логи выполнения
SELECT * FROM cron.job_run_details 
WHERE jobid IN (
  SELECT jobid FROM cron.job 
  WHERE jobname = 'update-appointment-statuses'
)
ORDER BY start_time DESC;
```

### Триггеры не срабатывают:
```sql
-- Проверьте, что триггеры созданы
SELECT * FROM information_schema.triggers 
WHERE trigger_schema = 'public';

-- Проверьте функции
SELECT proname FROM pg_proc 
WHERE proname LIKE 'handle_%';
```

### Очереди не работают:
```sql
-- Проверьте расширение
SELECT * FROM pg_extension WHERE extname = 'pgmq';

-- Проверьте очереди
SELECT * FROM pgmq.list_queues();
```

## 📚 Дополнительные ресурсы

- [Supabase Cron Documentation](https://supabase.com/docs/guides/database/extensions/pg_cron)
- [Supabase Queues Documentation](https://supabase.com/docs/guides/database/extensions/pgmq)
- [Database Webhooks Documentation](https://supabase.com/docs/guides/database/webhooks)



