# 🗄️ Применение миграции 039: Добавление поля is_featured

## ⚠️ Важно
Эта миграция добавляет поле `is_featured` в таблицу `blog_posts`, которое позволяет выбирать статью для hero-секции.

## 📍 Шаг 1: Откройте SQL Editor в Supabase

1. В админ-панели Supabase перейдите в **SQL Editor**
2. Нажмите **"New query"** (Новый запрос)

## 📝 Шаг 2: Примените миграцию

1. Откройте файл `supabase/migrations/039_add_is_featured_to_blog_posts.sql`
2. **Выделите весь текст** в файле (Ctrl+A)
3. **Скопируйте** (Ctrl+C)
4. Вставьте в SQL Editor в Supabase (Ctrl+V)
5. Нажмите **Run** (Выполнить) или `Ctrl+Enter`
6. Дождитесь сообщения **"Success"** или **"Успешно"**

## ✅ Шаг 3: Проверка

После применения миграции выполните проверку:

```sql
-- Проверка, что колонка создана
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'blog_posts' 
AND column_name = 'is_featured';
```

Должен вернуться результат с колонкой `is_featured` типа `boolean` с дефолтным значением `false`.

## 🔄 Шаг 4: После применения миграции

После успешного применения миграции нужно:

1. **Раскомментировать код** в `src/pages/admin/BlogManagement.tsx`:
   - Убрать комментарии вокруг переключателя "Главный материал (Hero)"
   - Убрать комментарии вокруг колонки "Hero" в таблице
   - Вернуть логику с `is_featured` в функции `upsertPost`

2. **Вернуть использование `is_featured`** в `src/pages/Blog.tsx`:
   - Раскомментировать строку с поиском featured статьи

## ⚠️ Если возникли ошибки

### Ошибка "column already exists":
- Это значит, что колонка уже существует
- Можно пропустить эту миграцию

### Другие ошибки:
- Скопируйте текст ошибки
- Сообщите мне, и я помогу исправить

## 🎉 Готово!

После применения миграции:
- ✅ Колонка `is_featured` создана
- ✅ Индекс для быстрого поиска создан
- ✅ Можно выбирать статью для hero-секции в админке
