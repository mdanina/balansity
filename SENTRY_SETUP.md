# 🔍 Настройка Sentry для мониторинга ошибок

## 📋 Что такое Sentry

Sentry - это сервис для мониторинга ошибок в production. Он автоматически отслеживает:
- JavaScript ошибки в браузере
- Ошибки React компонентов (через ErrorBoundary)
- Производительность приложения
- Session Replay (запись сессий с ошибками)

## 🚀 Быстрая настройка

### Шаг 1: Создайте проект в Sentry

1. Зарегистрируйтесь на [sentry.io](https://sentry.io)
2. Создайте новый проект:
   - Platform: **React**
   - Framework: **React**
3. Скопируйте **DSN** (Data Source Name) - он понадобится для настройки

### Шаг 2: Добавьте DSN в переменные окружения

Создайте или обновите файл `.env.production`:

```env
# Sentry DSN (обязательно для production)
VITE_SENTRY_DSN=https://your-key@sentry.io/your-project-id

# Опционально: включить Sentry в dev режиме для тестирования
VITE_SENTRY_ENABLE_IN_DEV=false
```

**Важно:** 
- Замените `your-key@sentry.io/your-project-id` на ваш реальный DSN
- DSN можно найти в настройках проекта Sentry: Settings → Client Keys (DSN)

### Шаг 3: Установите зависимости

```bash
npm install @sentry/react
```

### Шаг 4: Пересоберите проект

```bash
npm run build
```

## ✅ Проверка работы

### 1. Тестовая ошибка

Добавьте временно в код для проверки:

```typescript
// В любом компоненте
useEffect(() => {
  if (import.meta.env.VITE_SENTRY_DSN) {
    throw new Error('Test Sentry error');
  }
}, []);
```

После деплоя эта ошибка должна появиться в Sentry Dashboard.

### 2. Проверка в Sentry Dashboard

1. Откройте [sentry.io](https://sentry.io)
2. Перейдите в ваш проект
3. В разделе **Issues** должны появиться ошибки

## 🔧 Настройки

### Отключение в development

По умолчанию Sentry работает только в production. Для включения в dev:

```env
VITE_SENTRY_ENABLE_IN_DEV=true
```

### Настройка sampling rate

В файле `src/lib/sentry.ts` можно настроить:

- `tracesSampleRate` - процент транзакций для Performance Monitoring (по умолчанию 10% в production)
- `replaysSessionSampleRate` - процент сессий для Session Replay (по умолчанию 10% в production)
- `replaysOnErrorSampleRate` - процент сессий с ошибками (100% по умолчанию)

### Фильтрация ошибок

В `src/lib/sentry.ts` уже настроена фильтрация:
- Ошибки от браузерных расширений игнорируются
- Можно добавить дополнительные фильтры в `beforeSend`

## 📊 Что отслеживается

### Автоматически:
- ✅ Ошибки React компонентов (через ErrorBoundary)
- ✅ Критические ошибки из `logger.error()`
- ✅ JavaScript ошибки в браузере
- ✅ Performance метрики (10% транзакций)
- ✅ Session Replay для сессий с ошибками

### Вручную:

```typescript
import { captureException, captureMessage } from '@/lib/sentry';

// Отправить исключение
try {
  // код
} catch (error) {
  captureException(error, { context: 'additional info' });
}

// Отправить сообщение
captureMessage('Something important happened', 'warning');
```

## 🔐 Безопасность

- ✅ DSN - публичный ключ, безопасно хранить в коде
- ✅ Session Replay маскирует текст и медиа по умолчанию
- ✅ Не отправляются данные авторизации
- ✅ Можно настроить фильтрацию чувствительных данных

## 💰 Стоимость

- **Free план:** 5,000 ошибок/месяц, 10,000 performance units
- **Team план:** от $26/месяц
- Для большинства проектов Free плана достаточно

## 🚨 Troubleshooting

### Sentry не отправляет ошибки

1. Проверьте, что `VITE_SENTRY_DSN` установлен в `.env.production`
2. Убедитесь, что проект собран в production режиме: `npm run build`
3. Проверьте консоль браузера на ошибки подключения к Sentry
4. Убедитесь, что домен не блокируется CORS

### Ошибки не появляются в Sentry

1. Проверьте фильтры в `beforeSend` - возможно, ошибки фильтруются
2. Убедитесь, что вы в production режиме (или `VITE_SENTRY_ENABLE_IN_DEV=true`)
3. Проверьте настройки проекта в Sentry Dashboard

### Слишком много ошибок

1. Настройте фильтрацию в `beforeSend`
2. Уменьшите `tracesSampleRate` и `replaysSessionSampleRate`
3. Используйте `ignoreErrors` в конфигурации Sentry

---

**После настройки все ошибки в production будут автоматически отправляться в Sentry!** ✅


