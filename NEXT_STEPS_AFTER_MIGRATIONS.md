# 🎯 Следующие шаги после применения миграций

## ✅ Что уже работает автоматически

После применения миграций следующие процессы работают **автоматически**:

### 1. Автоматическое обновление статусов консультаций
- **Когда:** Каждые 5 минут
- **Что делает:** Обновляет статусы (`scheduled` → `in_progress` → `completed`)
- **Статус:** ✅ Работает автоматически

### 2. Автоматическая обработка завершенных чекапов
- **Когда:** При завершении чекапа (статус меняется на `completed`)
- **Что делает:** 
  - Добавляет задачу генерации отчета в очередь ✅
  - Добавляет задачу отправки email в очередь ✅
  - Обновляет аналитику ✅
- **Статус:** ✅ Триггеры работают, задачи добавляются в очереди

### 3. Автоматические уведомления о записях
- **Когда:** При создании новой записи на консультацию
- **Что делает:** Добавляет задачу отправки подтверждения в очередь ✅
- **Статус:** ✅ Триггеры работают, задачи добавляются в очереди

### 4. Автоматическая обработка платежей
- **Когда:** При изменении статуса платежа
- **Что делает:** Добавляет задачи уведомлений в очередь ✅
- **Статус:** ✅ Триггеры работают, задачи добавляются в очереди

---

## ⚠️ Что нужно настроить дальше

### 1. Проверка установки (5 минут)

Выполните SQL-запросы из файла `VERIFY_INTEGRATIONS.md` для проверки:
- Cron jobs созданы и активны
- Очереди созданы
- Функции работают
- Триггеры установлены

**Если все проверки прошли успешно** → переходите к следующему шагу.

---

### 2. Настройка обработчиков очередей (Workers) - ВАЖНО!

**Проблема:** Задачи добавляются в очереди, но **не выполняются автоматически**. Нужны обработчики (workers), которые будут:
- Читать задачи из очередей
- Выполнять их (отправлять email, генерировать отчеты)
- Отмечать как выполненные

**Варианты решения:**

#### Вариант A: Supabase Edge Functions (рекомендуется)

Создайте Edge Function для обработки очередей:

```typescript
// supabase/functions/process-email-queue/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

serve(async (req) => {
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? ''
  )

  // Читаем задачу из очереди
  const { data: task } = await supabase.rpc('pgmq_read', {
    queue_name: 'email_queue',
    vt: 30 // timeout 30 секунд
  })

  if (task) {
    // Отправляем email через ваш SMTP сервис
    await sendEmail(task.msg)
    
    // Архивируем задачу
    await supabase.rpc('pgmq_archive', {
      queue_name: 'email_queue',
      msg_id: task.msg_id
    })
  }

  return new Response(JSON.stringify({ success: true }), {
    headers: { 'Content-Type': 'application/json' }
  })
})
```

Затем настройте Cron job для вызова этой функции каждые 1-5 минут.

#### Вариант B: Отдельный сервис/worker

Создайте отдельный Node.js/Python сервис, который будет периодически читать очереди и обрабатывать задачи.

#### Вариант C: Временное решение - обработка в приложении

Можно добавить обработку очередей в ваше React приложение (не рекомендуется для продакшена, но подходит для тестирования).

---

### 3. Настройка SMTP для отправки email

**Проблема:** Задачи email добавляются в очередь, но для их выполнения нужен SMTP сервер.

**Варианты:**

#### Вариант A: Beget SMTP (если используете Beget)

```env
SMTP_HOST=smtp.beget.com
SMTP_PORT=587
SMTP_USER=ваш-email@beget.com
SMTP_PASS=ваш-пароль
SMTP_FROM=noreply@ваш-домен.com
```

#### Вариант B: Внешние сервисы

- **SendGrid** (бесплатно до 100 писем/день)
- **Mailgun** (бесплатно до 5000 писем/месяц)
- **AWS SES** (очень дешево)
- **Resend** (современный, простой API)

**Пример интеграции с Resend:**

```typescript
import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

async function sendEmail(task: any) {
  await resend.emails.send({
    from: 'noreply@ваш-домен.com',
    to: task.msg.to,
    subject: task.msg.subject,
    html: renderTemplate(task.msg.template, task.msg.variables)
  });
}
```

---

### 4. Мониторинг и отладка

#### Проверка работы Cron jobs:

```sql
-- Последние выполнения задач
SELECT 
  jobid,
  runid,
  job_pid,
  database,
  username,
  command,
  status,
  return_message,
  start_time,
  end_time
FROM cron.job_run_details
ORDER BY start_time DESC
LIMIT 10;
```

#### Проверка очередей:

```sql
-- Статистика очередей
SELECT 
  queue_name,
  queue_length,
  newest_msg_age_sec,
  oldest_msg_age_sec,
  total_messages
FROM pgmq_metrics('email_queue');
```

#### Проверка задач в очередях:

```sql
-- Необработанные задачи в очереди email
SELECT 
  msg_id,
  enqueued_at,
  msg->>'to' as email_to,
  msg->>'subject' as subject,
  EXTRACT(EPOCH FROM (now() - enqueued_at)) as age_seconds
FROM pgmq.email_queue
ORDER BY enqueued_at DESC
LIMIT 10;
```

---

## 📋 Чек-лист следующих шагов

- [ ] **Проверить установку** (SQL-запросы из VERIFY_INTEGRATIONS.md)
- [ ] **Настроить обработчик очередей** (Edge Function или отдельный сервис)
- [ ] **Настроить SMTP** (Beget, SendGrid, Mailgun или другой)
- [ ] **Протестировать** (завершить тестовый чекап и проверить, что задача добавилась в очередь)
- [ ] **Настроить мониторинг** (проверять очереди и Cron jobs регулярно)

---

## 🧪 Тестирование

### Тест 1: Проверка триггера при завершении чекапа

1. Завершите тестовый чекап в приложении
2. Проверьте очередь:

```sql
SELECT * FROM pgmq.report_generation_queue 
ORDER BY enqueued_at DESC LIMIT 1;

SELECT * FROM pgmq.email_queue 
WHERE msg->>'template' = 'checkup_completed'
ORDER BY enqueued_at DESC LIMIT 1;
```

Должны появиться задачи в очередях.

### Тест 2: Проверка Cron job

Подождите 5 минут после применения миграции, затем проверьте:

```sql
SELECT * FROM cron.job_run_details 
WHERE jobid = (SELECT jobid FROM cron.job WHERE jobname = 'update-appointment-statuses')
ORDER BY start_time DESC LIMIT 1;
```

Должна быть запись о выполнении задачи.

---

## 📚 Дополнительные ресурсы

- `VERIFY_INTEGRATIONS.md` - проверка установки
- `INTEGRATIONS_SETUP.md` - полная документация
- `APPLY_INTEGRATIONS.md` - инструкция по применению

---

## ❓ Вопросы?

Если что-то не работает:
1. Проверьте логи Cron jobs
2. Проверьте очереди на наличие задач
3. Проверьте, что триггеры установлены
4. Проверьте права доступа к функциям






