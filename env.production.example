# ============================================
# Конфигурация фронтенда для продакшена
# ============================================
# Скопируйте этот файл в .env.production и заполните реальными значениями

# Supabase конфигурация
VITE_SUPABASE_URL=https://oyuyienekon.beget.app
VITE_SUPABASE_ANON_KEY=ваш-anon-key-здесь

# API URL для платежей (queue-worker)
# Замените на ваш домен
# ВАЖНО: путь /worker нужен т.к. Beget перехватывает /api для Supabase
VITE_API_URL=https://ваш-домен.com/worker

# Sentry для мониторинга ошибок (опционально)
# Получите DSN в https://sentry.io после создания проекта
# VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id
# VITE_SENTRY_ENABLE_IN_DEV=false  # true для тестирования в dev режиме

# ============================================
# Где получить значения:
# ============================================
# 
# VITE_SUPABASE_URL:
#   - URL вашего Supabase проекта
#   - Обычно: https://oyuyienekon.beget.app
# 
# VITE_SUPABASE_ANON_KEY:
#   - В админ-панели Supabase: Settings → API → anon public key
#   - ⚠️ Используйте anon key, НЕ service_role!
# 
# VITE_API_URL:
#   - URL вашего API (queue-worker)
#   - Должен быть доступен через Nginx прокси: /worker
#   - Формат: https://ваш-домен.com/worker
#   - ВАЖНО: используем /worker вместо /api т.к. Beget перехватывает /api
# 
# ============================================
# Важно:
# ============================================
# - Файл должен называться .env.production (с точкой в начале)
# - После изменения пересоберите проект: npm run build
# - Не коммитьте этот файл в Git (должен быть в .gitignore)
# 
# ============================================

