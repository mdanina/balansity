# 📧 Настройка Email-подтверждения для Self-Hosted Supabase

Для self-hosted Supabase настройки email делаются через **SQL команды** напрямую в базе данных.

## ⚠️ Важно: Для Self-Hosted Supabase

В self-hosted Supabase настройки email **НЕ хранятся в таблице `auth.config`**. 
Вместо этого они настраиваются через **переменные окружения GoTrue** (auth сервиса).

## 🔧 Шаг 1: Отключить подтверждение email (для разработки)

### Способ 1: Через переменные окружения (рекомендуется)

Если у вас есть доступ к серверу и конфигурационным файлам:

#### Для Docker Compose:

1. Найдите файл `docker-compose.yml` на сервере
2. Найдите секцию `auth` (или `gotrue`)
3. Добавьте/измените переменные окружения:

```yaml
services:
  auth:
    environment:
      GOTRUE_MAILER_AUTOCONFIRM: "true"  # true = без подтверждения, false = с подтверждением
      GOTRUE_SITE_URL: "http://localhost:8080"
```

4. Перезапустите контейнер:
```bash
docker-compose restart auth
# или
docker-compose down && docker-compose up -d
```

#### Для .env файла:

1. Найдите файл `.env` на сервере (обычно в папке с Supabase)
2. Добавьте/измените:

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=http://localhost:8080
```

3. Перезапустите сервисы

### Способ 2: Через настройки в панели управления Beget

Если Supabase установлен через панель Beget:

1. Войдите в панель управления Beget
2. Найдите установленное приложение Supabase
3. Перейдите в настройки приложения
4. Найдите раздел "Environment Variables" или "Переменные окружения"
5. Добавьте/измените:
   - `GOTRUE_MAILER_AUTOCONFIRM=true` (для отключения подтверждения)
   - `GOTRUE_SITE_URL=http://localhost:8080`
6. Сохраните и перезапустите приложение

## 🔧 Шаг 2: Включить подтверждение email (для продакшена)

Для включения подтверждения email установите:

```env
GOTRUE_MAILER_AUTOCONFIRM=false
```

И настройте SMTP (см. Шаг 3).

## 📧 Шаг 3: Настройка SMTP

Для настройки SMTP добавьте переменные окружения:

### Для Docker Compose:

```yaml
services:
  auth:
    environment:
      GOTRUE_SMTP_HOST: smtp.beget.com
      GOTRUE_SMTP_PORT: 587
      GOTRUE_SMTP_USER: ваш-email@beget.com
      GOTRUE_SMTP_PASS: ваш-пароль
      GOTRUE_SMTP_ADMIN_EMAIL: noreply@ваш-домен.com
      GOTRUE_SMTP_SENDER_NAME: "Little Otter"
```

### Для .env файла:

```env
GOTRUE_SMTP_HOST=smtp.beget.com
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Little Otter
```

**Важно:** 
- Пароль храните в безопасном месте
- После изменения перезапустите сервис auth

## 🔧 Шаг 4: Настройка Site URL и Redirect URLs

Для правильной работы подтверждения email настройте URL через переменные окружения (см. Шаг 1 и Шаг 2 выше).

## 🔍 Шаг 5: Проверка текущих настроек

Чтобы проверить текущие настройки:

### Через Docker:

```bash
# Посмотреть переменные окружения контейнера auth
docker exec <container_name> env | grep GOTRUE

# Или если используете docker-compose
docker-compose exec auth env | grep GOTRUE
```

### Через файлы:

Проверьте файл `.env` или `docker-compose.yml` на наличие переменных `GOTRUE_*`

## 🚨 Альтернативный способ: Через конфигурационные файлы

Если у вас есть доступ к файлам на сервере, настройки можно изменить в:

### Docker Compose (если используется)

Найдите файл `docker-compose.yml` и обновите переменные окружения:

```yaml
services:
  auth:
    environment:
      GOTRUE_SITE_URL: http://localhost:8080
      GOTRUE_MAILER_ENABLED: "true"
      GOTRUE_MAILER_AUTOCONFIRM: "false"  # true = без подтверждения, false = с подтверждением
      GOTRUE_SMTP_HOST: smtp.beget.com
      GOTRUE_SMTP_PORT: 587
      GOTRUE_SMTP_USER: ваш-email@beget.com
      GOTRUE_SMTP_PASS: ваш-пароль
      GOTRUE_SMTP_ADMIN_EMAIL: noreply@ваш-домен.com
      GOTRUE_SMTP_SENDER_NAME: "Little Otter"
```

После изменения перезапустите контейнеры:
```bash
docker-compose down
docker-compose up -d
```

### .env файл (если используется)

Найдите файл `.env` на сервере и добавьте/измените:

```env
GOTRUE_SITE_URL=http://localhost:8080
GOTRUE_MAILER_ENABLED=true
GOTRUE_MAILER_AUTOCONFIRM=false
GOTRUE_SMTP_HOST=smtp.beget.com
GOTRUE_SMTP_PORT=587
GOTRUE_SMTP_USER=ваш-email@beget.com
GOTRUE_SMTP_PASS=ваш-пароль
GOTRUE_SMTP_ADMIN_EMAIL=noreply@ваш-домен.com
GOTRUE_SMTP_SENDER_NAME=Little Otter
```

## 📝 Шаг 6: Пример полной конфигурации

### Для разработки (без подтверждения email):

**docker-compose.yml:**
```yaml
services:
  auth:
    environment:
      GOTRUE_SITE_URL: "http://localhost:8080"
      GOTRUE_MAILER_AUTOCONFIRM: "true"  # Без подтверждения
```

**Или .env:**
```env
GOTRUE_SITE_URL=http://localhost:8080
GOTRUE_MAILER_AUTOCONFIRM=true
```

### Для продакшена (с подтверждением email и SMTP):

**docker-compose.yml:**
```yaml
services:
  auth:
    environment:
      GOTRUE_SITE_URL: "https://ваш-домен.com"
      GOTRUE_MAILER_AUTOCONFIRM: "false"  # С подтверждением
      GOTRUE_SMTP_HOST: smtp.beget.com
      GOTRUE_SMTP_PORT: 587
      GOTRUE_SMTP_USER: ваш-email@beget.com
      GOTRUE_SMTP_PASS: ваш-пароль
      GOTRUE_SMTP_ADMIN_EMAIL: noreply@ваш-домен.com
      GOTRUE_SMTP_SENDER_NAME: "Little Otter"
```

## ✅ Проверка работы

1. Выполните SQL команды в SQL Editor
2. Попробуйте зарегистрировать тестового пользователя
3. Если подтверждение отключено - пользователь сразу войдет в систему
4. Если подтверждение включено - проверьте почту (включая спам)

## ⚠️ Важные замечания

1. **Для разработки:** Рекомендуется отключить подтверждение email для удобства тестирования
2. **Для продакшена:** Обязательно включите подтверждение email и настройте SMTP
3. **Безопасность:** Не храните пароли SMTP в открытом виде в SQL
4. **Beget SMTP:** Убедитесь, что у вас есть доступ к SMTP на Beget и правильные учетные данные

## 🔍 Если не работает

1. **Проверьте, что переменные окружения установлены:**
   ```bash
   docker-compose exec auth env | grep GOTRUE
   ```

2. **Убедитесь, что сервис auth перезапущен** после изменения переменных:
   ```bash
   docker-compose restart auth
   ```

3. **Проверьте логи auth сервиса:**
   ```bash
   docker-compose logs auth
   ```

4. **Если используете Beget:**
   - Убедитесь, что у вас есть доступ к настройкам приложения
   - Проверьте, что переменные окружения сохранились
   - Попробуйте перезапустить приложение через панель Beget

5. **Альтернативный способ - через API:**
   - Некоторые настройки можно изменить через Management API
   - Используйте `service_role` ключ для доступа к API

6. **Проверьте документацию GoTrue:**
   - Self-hosted Supabase использует GoTrue для аутентификации
   - Все переменные окружения начинаются с `GOTRUE_`
   - Полный список: https://github.com/supabase/gotrue

