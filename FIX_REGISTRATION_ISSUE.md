# 🔧 Решение проблемы с регистрацией

## Проблема

Пользователь не создается при регистрации из-за ошибки отправки email.

## Причина

Supabase пытается отправить email для подтверждения, но не может (SMTP не настроен или не работает), и из-за этого не создает пользователя.

## Решение

### Вариант 1: Настроить переменные на сервере (ОБЯЗАТЕЛЬНО)

В файле `/opt/beget/supabase/.env` должны быть:

```env
ENABLE_EMAIL_AUTOCONFIRM=true
GOTRUE_MAILER_AUTOCONFIRM=true
SITE_URL=https://oyuyienekon.beget.app
```

**ВАЖНО:** После изменения `.env` нужно перезапустить контейнер `auth`!

### Вариант 2: Изменить docker-compose.yml напрямую

Если `.env` не работает, откройте `/opt/beget/supabase/docker-compose.yml` и найдите секцию `auth`:

```yaml
services:
  auth:
    environment:
      ENABLE_EMAIL_AUTOCONFIRM: "true"
      GOTRUE_MAILER_AUTOCONFIRM: "true"
      SITE_URL: "https://oyuyienekon.beget.app"
      # Убедитесь, что эти переменные есть
```

### Вариант 3: Обратиться в поддержку Beget

Если нет доступа к перезапуску контейнера, напишите в поддержку:

"Здравствуйте! Мне нужно перезапустить Docker-контейнер supabase-auth на сервере. 
Файл конфигурации находится в `/opt/beget/supabase/`. 
Нужно выполнить команду: `cd /opt/beget/supabase && docker-compose restart auth`

Также нужно убедиться, что в файле `.env` установлены переменные:
- ENABLE_EMAIL_AUTOCONFIRM=true
- GOTRUE_MAILER_AUTOCONFIRM=true
- SITE_URL=https://oyuyienekon.beget.app

Спасибо!"

## Проверка

После перезапуска:
1. Откройте консоль браузера (F12)
2. Попробуйте зарегистрироваться
3. В консоли должны быть логи: `Registration result:`
4. Если пользователь создается, вы увидите `User created successfully`

## Что изменилось в коде

Код теперь:
- Логирует все ответы от Supabase в консоль
- Пытается войти автоматически, если есть ошибка email
- Показывает более детальные сообщения об ошибках

## Важно

Без перезапуска контейнера `auth` изменения в `.env` НЕ применятся! Это критично для работы регистрации.








