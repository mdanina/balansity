# ✅ Проверка настроек .env на сервере

## 🔍 Что проверить в файле `/opt/beget/supabase/.env`

### 1. Убедитесь, что эти строки есть и правильные:

```env
ENABLE_EMAIL_AUTOCONFIRM=true
GOTRUE_MAILER_AUTOCONFIRM=true
SITE_URL=https://oyuyienekon.beget.app
```

### 2. Проверьте, что НЕТ конфликтующих настроек:

Убедитесь, что нет строки:
```env
ENABLE_EMAIL_AUTOCONFIRM=false  # ❌ Это должно быть true
```

### 3. Проверьте секцию SMTP

Если SMTP не настроен правильно, это может вызывать ошибки. Можно временно отключить отправку писем:

```env
SMTP_HOST=supabase-mail
SMTP_PORT=2500
# Эти настройки должны быть для локального тестирования
```

## 🔧 Альтернативное решение: Изменить docker-compose.yml

Если изменения в .env не работают, попробуйте изменить `docker-compose.yml` напрямую:

1. Откройте файл `/opt/beget/supabase/docker-compose.yml`
2. Найдите секцию `auth:` (или `gotrue:`)
3. В секции `environment:` добавьте/измените:

```yaml
services:
  auth:
    environment:
      ENABLE_EMAIL_AUTOCONFIRM: "true"
      GOTRUE_MAILER_AUTOCONFIRM: "true"
      SITE_URL: "https://oyuyienekon.beget.app"
```

## ⚠️ Важно

После ЛЮБЫХ изменений в `.env` или `docker-compose.yml` нужно перезапустить контейнер auth. Без перезапуска изменения не применятся!

## 🆘 Если перезапуск недоступен

Обратитесь в поддержку Beget с просьбой:
"Пожалуйста, перезапустите Docker-контейнер supabase-auth на сервере. Команда: `cd /opt/beget/supabase && docker-compose restart auth`"



