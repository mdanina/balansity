# Как проверить .env файл

## 🔍 Быстрая проверка

### Вариант 1: Автоматическая проверка (рекомендуется)

Запустите скрипт проверки:

```bash
node check-env.js
```

Скрипт покажет:
- ✅ Найден ли файл .env
- ✅ Какие переменные установлены
- ❌ Какие переменные отсутствуют

---

### Вариант 2: Ручная проверка

#### Шаг 1: Найти файл

Файл должен быть в **корне проекта** (рядом с `package.json`) и называться:
- `.env` (основной)
- `.env.local` (тоже работает в Vite)
- `.env.development` (для dev режима)

#### Шаг 2: Открыть файл

Откройте файл в любом текстовом редакторе.

#### Шаг 3: Проверить содержимое

Файл должен содержать две строки:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## ✅ Что проверить

### 1. Формат переменных

✅ **Правильно:**
```env
VITE_SUPABASE_URL=https://oyuyienekon.beget.app
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY0Mjg4MDAwLCJleHAiOjE5MjIwNTQ0MDB9.tQMuow03daqPpOTnwdMwOma5CNvzVpH1s_pTBxqPdZk
```

❌ **Неправильно:**
```env
# Без префикса VITE_
SUPABASE_URL=...              # ❌ Не работает

# С пробелами
VITE_SUPABASE_URL = ...       # ❌ Не работает

# Без значения
VITE_SUPABASE_URL=            # ❌ Пустое значение
```

### 2. Значения

**VITE_SUPABASE_URL:**
- ✅ Должен начинаться с `https://`
- ✅ Должен заканчиваться на `.supabase.co` или `.beget.app` (для self-hosted)

**VITE_SUPABASE_ANON_KEY:**
- ✅ Длинная строка (обычно 200+ символов)
- ✅ Начинается с `eyJ` (JWT токен)
- ✅ **НЕ** service_role ключ!

---

## 🧪 Тестирование

### Проверка при запуске

1. Запустите приложение:
   ```bash
   npm run dev
   ```

2. Если переменные **правильно** установлены:
   - ✅ Приложение запустится
   - ✅ Никаких ошибок в консоли
   - ✅ Страница загрузится

3. Если переменные **отсутствуют или неправильные**:
   - ❌ Приложение выбросит ошибку при запуске
   - ❌ В консоли будет ошибка:
     ```
     Error: Критические переменные окружения не настроены:
     VITE_SUPABASE_URL не установлена
     ```

---

## 📝 Создать .env (если файла нет)

### Шаг 1: Создать файл

В корне проекта создайте файл `.env` (с точкой в начале!)

**Windows:**
- В VS Code: Файл → Новый файл → Назвать `.env`
- В Notepad: Сохранить как → Тип: "Все файлы" → Имя: `.env`

### Шаг 2: Заполнить значения

Откройте файл и добавьте:

```env
VITE_SUPABASE_URL=ваш_url_здесь
VITE_SUPABASE_ANON_KEY=ваш_ключ_здесь
```

**Где взять значения?**

См. файл `HOW_TO_GET_API_KEY.md` или:
1. Откройте Supabase Dashboard
2. Project Settings → API
3. Скопируйте **Project URL** → в `VITE_SUPABASE_URL`
4. Скопируйте **anon/public key** → в `VITE_SUPABASE_ANON_KEY`

### Шаг 3: Сохранить

Убедитесь, что файл сохранен как `.env` (не `.env.txt`!)

---

## 🔍 Проверка через код

Можно также проверить в консоли браузера после запуска приложения:

```javascript
// Откройте консоль браузера (F12) и выполните:
console.log(import.meta.env.VITE_SUPABASE_URL);
console.log(import.meta.env.VITE_SUPABASE_ANON_KEY);

// Если видите значения - всё ОК!
// Если видите undefined - переменные не загружены
```

**Внимание:** Это работает только в dev режиме, и ключи будут видны в консоли (это нормально для anon key).

---

## ⚠️ Частые ошибки

### Ошибка: "Файл не читается"
- Убедитесь, что файл называется `.env` (не `.env.txt` или `.env.local.txt`)
- Убедитесь, что файл в корне проекта (рядом с `package.json`)

### Ошибка: "Переменные не загружаются"
- **Перезапустите** dev сервер (`Ctrl+C` и снова `npm run dev`)
- В Vite переменные загружаются только при старте

### Ошибка: "URL имеет неверный формат"
- Проверьте, что URL начинается с `https://`
- Убедитесь, что нет лишних пробелов или символов

---

## 📋 Чек-лист

- [ ] Файл `.env` существует в корне проекта
- [ ] Файл содержит `VITE_SUPABASE_URL=...`
- [ ] Файл содержит `VITE_SUPABASE_ANON_KEY=...`
- [ ] URL начинается с `https://`
- [ ] Anon key начинается с `eyJ...`
- [ ] Нет пробелов вокруг знака `=`
- [ ] При запуске `npm run dev` нет ошибок о переменных

---

**После проверки можно запускать проект! 🚀**










