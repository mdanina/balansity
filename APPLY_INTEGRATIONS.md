# 🚀 Применение интеграций Supabase

## Быстрый старт

### Шаг 1: Применить миграции

Откройте **Supabase Dashboard → SQL Editor** и выполните миграции по порядку:

#### 1. Настройка Cron jobs
```sql
-- Скопируйте и выполните содержимое файла:
-- supabase/migrations/030_setup_cron_jobs.sql
```

#### 2. Настройка очередей
```sql
-- Скопируйте и выполните содержимое файла:
-- supabase/migrations/031_setup_queues.sql
```

#### 3. Настройка триггеров
```sql
-- Скопируйте и выполните содержимое файла:
-- supabase/migrations/032_webhook_triggers.sql
```

### Шаг 2: Проверка

Выполните эти запросы для проверки:

```sql
-- Проверка Cron jobs
SELECT jobname, schedule, active 
FROM cron.job 
WHERE jobname IN (
  'update-appointment-statuses', 
  'cleanup-old-assessments', 
  'send-appointment-reminders'
);

-- Проверка очередей
SELECT queue_name FROM pgmq.list_queues();

-- Проверка триггеров
SELECT trigger_name, event_object_table 
FROM information_schema.triggers 
WHERE trigger_schema = 'public' 
  AND trigger_name IN (
    'on_checkup_completed', 
    'on_new_appointment', 
    'on_payment_status_change'
  );
```

## ✅ Что теперь работает автоматически

1. ✅ **Статусы консультаций** обновляются каждые 5 минут
2. ✅ **При завершении чекапа** автоматически:
   - Генерируется отчет (в очереди)
   - Отправляется email пользователю
   - Обновляется аналитика
3. ✅ **При создании записи** отправляется подтверждение
4. ✅ **Напоминания** за 24 часа и 1 час до консультации
5. ✅ **При оплате** автоматически обновляются статусы и отправляются уведомления

## 📝 Следующие шаги

1. **Настройте обработчики очередей** (workers) для выполнения задач
   - См. `INTEGRATIONS_SETUP.md` для примеров
   
2. **Настройте SMTP** для отправки email
   - Или используйте внешний сервис (SendGrid, Mailgun и т.д.)

3. **Мониторинг**: Регулярно проверяйте очереди и Cron jobs

## ⚠️ Важно

- Миграции нужно применять **по порядку** (030 → 031 → 032)
- После применения миграций все работает **автоматически**
- Для обработки задач из очередей нужны **workers** (см. документацию)

## 📚 Подробная документация

См. `INTEGRATIONS_SETUP.md` для полной документации.









