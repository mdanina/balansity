# 🚀 Как запустить Queue Worker

## ✅ Зависимости установлены!

Вы уже выполнили `npm install` - отлично! Теперь можно запускать worker.

---

## 🧪 Запуск для тестирования (режим разработки)

В терминале выполните:

```bash
npm run dev
```

**Что произойдет:**
- Worker запустится
- В консоли появятся логи каждые 5 секунд
- Worker будет проверять очереди и обрабатывать задачи

**Ожидаемый вывод:**
```
[INFO] Queue Worker started
[INFO] Interval: 5000ms
[INFO] Max tasks per cycle: 10
[INFO] Starting queue processing cycle...
Processed: 0 emails, 0 reports, 0 payments
```

**Для остановки:** Нажмите `Ctrl+C`

---

## 🧪 Тестирование работы

### Тест 1: Проверка подключения

После запуска проверьте логи - не должно быть ошибок типа:
- ❌ "SUPABASE_SERVICE_ROLE_KEY must be set"
- ❌ "Error reading from queue"

Если есть ошибки - проверьте файл `.env`.

### Тест 2: Проверка обработки email

1. **Завершите тестовый чекап** в вашем приложении
2. **Подождите 5-10 секунд**
3. **В логах worker'а** должно появиться:
   ```
   [INFO] Processing email task: user@example.com - Ваш чекап завершен
   [INFO] Email sent via SMTP to user@example.com
   [INFO] Email sent successfully to user@example.com
   [INFO] Processed: 1 emails, 0 reports, 0 payments
   ```
4. **Проверьте почту** - должно прийти письмо от `noreply@balansity.ru`

---

## 🚀 Запуск в продакшене

### Вариант A: PM2 (рекомендуется)

```bash
# 1. Сборка проекта
npm run build

# 2. Запуск через PM2
npm run start:pm2

# 3. Сохранение для автозапуска
pm2 save
pm2 startup  # Следуйте инструкциям
```

**Управление:**
```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs balansity-queue-worker

# Остановка
npm run stop:pm2

# Перезапуск
npm run restart:pm2
```

### Вариант B: Запуск напрямую

```bash
npm run build
npm start
```

---

## 📊 Проверка работы

### Проверка логов

Если используете PM2:
```bash
pm2 logs balansity-queue-worker
```

Если запускаете напрямую - логи видны в консоли.

### Проверка очередей в Supabase

Выполните в SQL Editor:

```sql
-- Проверка задач в очереди email
SELECT 
  msg_id,
  enqueued_at,
  msg->>'to' as email_to,
  msg->>'subject' as subject,
  EXTRACT(EPOCH FROM (now() - enqueued_at)) as age_seconds
FROM pgmq.email_queue
ORDER BY enqueued_at DESC
LIMIT 10;
```

**Если worker работает:**
- Задачи должны исчезать из очереди после обработки
- В логах worker'а должны быть записи об обработке

---

## ⚠️ Решение проблем

### Worker не запускается

**Ошибка:** "SUPABASE_SERVICE_ROLE_KEY must be set"
- ✅ Решение: Проверьте файл `.env` - ключ должен быть заполнен

**Ошибка:** "Cannot find module"
- ✅ Решение: Выполните `npm install` еще раз

### Worker запускается, но не обрабатывает задачи

**Проверьте:**
1. Есть ли задачи в очередях:
   ```sql
   SELECT * FROM pgmq.email_queue LIMIT 5;
   ```
2. Правильно ли заполнен `SUPABASE_SERVICE_ROLE_KEY`
3. Логи worker'а на наличие ошибок

### Email не отправляются

**Проверьте:**
1. SMTP настройки в `.env`
2. Логи на ошибки SMTP (например, "authentication failed")
3. Существует ли почтовый ящик `noreply@balansity.ru`

---

## ✅ Готово!

После запуска `npm run dev` worker будет:
- ✅ Автоматически обрабатывать email очередь
- ✅ Отправлять уведомления пользователям
- ✅ Работать в фоне

**Worker работает постоянно** - не нужно его перезапускать! 🎉








