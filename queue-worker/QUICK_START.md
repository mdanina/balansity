# 🚀 Быстрый запуск Queue Worker

## Шаг 1: Установка зависимостей

Откройте терминал в папке `queue-worker` и выполните:

```bash
npm install
```

Это установит все необходимые пакеты.

---

## Шаг 2: Проверка .env файла

Убедитесь, что файл `.env` заполнен:

- ✅ `SUPABASE_URL` - должен быть `https://oyuyienekon.beget.app`
- ✅ `SUPABASE_SERVICE_ROLE_KEY` - должен быть заполнен (ваш service_role ключ)
- ✅ Все SMTP настройки заполнены

---

## Шаг 3: Запуск в режиме разработки (для тестирования)

```bash
npm run dev
```

**Что произойдет:**
- Worker запустится и начнет обрабатывать очереди
- Вы увидите логи в консоли
- Worker будет проверять очереди каждые 5 секунд
- При появлении задач - обработает их

**Для остановки:** Нажмите `Ctrl+C`

---

## Шаг 4: Тестирование

### Тест 1: Проверка подключения

После запуска вы должны увидеть в консоли:
```
[INFO] Queue Worker started
[INFO] Interval: 5000ms
[INFO] Max tasks per cycle: 10
[INFO] Starting queue processing cycle...
```

Если видите ошибки подключения - проверьте `SUPABASE_SERVICE_ROLE_KEY` в `.env`.

### Тест 2: Проверка обработки email

1. Завершите тестовый чекап в приложении
2. Подождите 5-10 секунд
3. В логах worker'а должно появиться:
   ```
   [INFO] Processing email task: user@example.com - Ваш чекап завершен
   [INFO] Email sent via SMTP to user@example.com
   [INFO] Email sent successfully to user@example.com
   ```
4. Проверьте почту - должно прийти письмо

---

## Шаг 5: Запуск в продакшене

### Вариант A: PM2 (рекомендуется для продакшена)

```bash
# 1. Сборка проекта
npm run build

# 2. Запуск через PM2
npm run start:pm2

# 3. Сохранение для автозапуска
pm2 save
pm2 startup  # Следуйте инструкциям на экране
```

**Управление:**
```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs balansity-queue-worker

# Остановка
npm run stop:pm2

# Перезапуск
npm run restart:pm2
```

### Вариант B: Docker

```bash
docker-compose up -d
```

**Просмотр логов:**
```bash
docker-compose logs -f queue-worker
```

### Вариант C: Запуск напрямую (для тестирования)

```bash
# Сборка
npm run build

# Запуск
npm start
```

---

## 📊 Проверка работы

### Проверка логов

```bash
# PM2
pm2 logs balansity-queue-worker

# Docker
docker-compose logs -f queue-worker

# Или файлы
tail -f queue-worker/logs/out.log
```

### Проверка очередей в Supabase

Выполните в SQL Editor:

```sql
-- Проверка задач в очереди email
SELECT 
  msg_id,
  enqueued_at,
  msg->>'to' as email_to,
  msg->>'subject' as subject
FROM pgmq.email_queue
ORDER BY enqueued_at DESC
LIMIT 10;
```

Если worker работает, задачи должны исчезать из очереди после обработки.

---

## ⚠️ Решение проблем

### Ошибка: "SUPABASE_SERVICE_ROLE_KEY must be set"

**Решение:** Проверьте файл `.env` - ключ должен быть заполнен.

### Ошибка: "Error reading from queue"

**Решение:** 
1. Проверьте подключение к Supabase
2. Убедитесь, что миграции 031 (очереди) применены
3. Проверьте, что очереди созданы:
   ```sql
   SELECT * FROM pgmq.list_queues();
   ```

### Email не отправляются

**Решение:**
1. Проверьте SMTP настройки в `.env`
2. Проверьте логи на ошибки SMTP
3. Убедитесь, что почтовый ящик `noreply@balansity.ru` существует

---

## ✅ Готово!

После запуска worker будет:
- ✅ Автоматически обрабатывать email очередь
- ✅ Отправлять уведомления пользователям
- ✅ Обрабатывать задачи генерации отчетов
- ✅ Обрабатывать задачи платежей

Worker работает в фоне и не требует вашего участия! 🎉




