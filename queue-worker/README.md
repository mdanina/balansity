# Balansity Queue Worker

Worker для обработки очередей Supabase (email, отчеты, платежи).

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd queue-worker
npm install
```

### 2. Настройка .env

Скопируйте `.env.example` в `.env`:

**Windows (PowerShell или CMD):**
```bash
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

Или создайте файл `.env` вручную и скопируйте содержимое из `.env.example`.

Затем заполните необходимые поля:

```env
SUPABASE_URL=https://oyuyienekon.beget.app
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key

# API Server
API_PORT=3001
API_BASE_URL=https://ваш-домен.com
FRONTEND_URL=https://ваш-домен.com

# YooKassa (тестовые ключи для разработки)
YUKASSA_SHOP_ID=1219382
YUKASSA_SECRET_KEY=test_UNRmTdMtTWuCm362Zkzn0xWhP8CVu_cvxKQ_jxjVXUg
# Для продакшна замените на реальные ключи из личного кабинета ЮKassa

# SMTP от nic.ru (те же данные, что в SMTP_SETUP_BALANSITY.md)
EMAIL_PROVIDER=smtp
SMTP_HOST=mail.nic.ru
SMTP_PORT=587
SMTP_USER=noreply@balansity.ru
SMTP_PASS=ILoveBalansity100!
SMTP_FROM=noreply@balansity.ru
SMTP_FROM_NAME=Little Otter
```

### 3. Запуск

#### Разработка:
```bash
npm run dev
```

#### Продакшен (PM2):
```bash
npm run build
npm run start:pm2
```

#### Продакшен (Docker):
```bash
docker-compose up -d
```

## 📋 Что делает worker

- **API сервер** - HTTP API для создания платежей и обработки webhook от ЮKassa
- **Обрабатывает email очередь** - отправляет уведомления пользователям
- **Обрабатывает очередь отчетов** - генерирует PDF отчеты (TODO)
- **Обрабатывает очередь платежей** - обрабатывает платежи (TODO)

## ⚙️ Настройки

- `API_PORT` - порт для API сервера (по умолчанию 3001)
- `API_BASE_URL` - базовый URL для webhook (должен быть доступен из интернета)
- `FRONTEND_URL` - URL фронтенда для CORS
- `YUKASSA_SHOP_ID` - ID магазина ЮKassa
- `YUKASSA_SECRET_KEY` - секретный ключ ЮKassa
- `WORKER_INTERVAL_MS` - интервал проверки очередей (по умолчанию 5000ms)
- `MAX_TASKS_PER_CYCLE` - максимум задач за один цикл (по умолчанию 10)
- `LOG_LEVEL` - уровень логирования (debug, info, warn, error)

### Настройки Retry (повторных попыток)

- `MAX_EMAIL_RETRY_ATTEMPTS` - максимум попыток для email задач (по умолчанию 5)
- `MAX_PAYMENT_RETRY_ATTEMPTS` - максимум попыток для платежей (по умолчанию 5)
- `MAX_REPORT_RETRY_ATTEMPTS` - максимум попыток для отчетов (по умолчанию 3)

**Как работает retry:**
- При ошибке задача возвращается в очередь с метаданными о попытке
- Используется exponential backoff: 1s, 2s, 4s, 8s, 16s... (максимум 60s)
- После превышения лимита задача архивируется как неудачная
- Предотвращает бесконечные циклы при постоянных ошибках

## 📝 Логи

Логи сохраняются в папке `logs/`:
- `error.log` - ошибки
- `out.log` - общий вывод

## 🔧 Troubleshooting

### Worker не обрабатывает задачи

1. Проверьте подключение к Supabase:
   ```bash
   # Проверьте переменные окружения
   cat .env | grep SUPABASE
   ```

2. Проверьте очереди в Supabase:
   ```sql
   SELECT * FROM pgmq.list_queues();
   ```

3. Проверьте логи:
   ```bash
   tail -f logs/out.log
   ```

### Email не отправляются

1. Проверьте SMTP настройки в `.env`
2. Проверьте логи на ошибки SMTP
3. Убедитесь, что почтовый ящик `noreply@balansity.ru` существует

### API сервер не отвечает

1. Проверьте, что порт не занят:
   ```bash
   lsof -i :3001
   ```

2. Проверьте переменные окружения:
   ```bash
   cat .env | grep API
   ```

3. Проверьте логи:
   ```bash
   tail -f logs/out.log
   ```

## 📚 Документация

Полная документация по интеграции ЮKassa: [YOOKASSA_INTEGRATION.md](../YOOKASSA_INTEGRATION.md)



