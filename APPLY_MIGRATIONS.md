# 🗄️ Применение SQL миграций в Supabase

## 📍 Шаг 1: Откройте SQL Editor

1. В админ-панели Supabase перейдите по адресу:
   ```
   https://oyuyienekon.beget.app/project/default/sql
   ```
   
   Или найдите в меню:
   - **SQL Editor** (Редактор SQL)
   - Или иконку с символом `</>` или `SQL`

2. Нажмите **"New query"** (Новый запрос) или просто начните вводить

---

## 📝 Шаг 2: Примените миграцию 001 (Создание таблиц)

### Что делает эта миграция:
- Создает таблицы: `users`, `profiles`, `assessments`, `answers`
- Создает индексы для быстрого поиска
- Создает триггеры для автоматического обновления `updated_at`

### Как применить:

1. Откройте файл `supabase/migrations/001_initial_schema.sql` в вашем проекте
2. **Выделите весь текст** в файле (Ctrl+A)
3. **Скопируйте** (Ctrl+C)
4. Вставьте в SQL Editor в Supabase (Ctrl+V)
5. Нажмите **Run** (Выполнить) или `Ctrl+Enter`
6. Дождитесь сообщения **"Success"** или **"Успешно"**

⚠️ **Важно:** Если увидите ошибки, скопируйте их текст и сообщите мне!

---

## 🔒 Шаг 3: Примените миграцию 002 (Безопасность RLS)

### Что делает эта миграция:
- Включает Row Level Security (RLS) для всех таблиц
- Создает политики безопасности, чтобы пользователи видели только свои данные

### Как применить:

1. Откройте файл `supabase/migrations/002_rls_policies.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь успешного выполнения

---

## ⚙️ Шаг 4: Примените миграцию 003 (Функции)

### Что делает эта миграция:
- Создает функции для подсчета баллов SDQ
- Создает функцию для завершения оценки
- Создает функцию для получения активной оценки

### Как применить:

1. Откройте файл `supabase/migrations/003_scoring_functions.sql`
2. Скопируйте весь SQL код
3. Вставьте в SQL Editor
4. Нажмите **Run**
5. Дождитесь успешного выполнения

---

## ✅ Шаг 5: Проверка

### Проверка таблиц:

1. В админ-панели найдите **Table Editor** (Редактор таблиц)
   - Обычно в меню слева или по адресу: `https://oyuyienekon.beget.app/project/default/editor`

2. Должны быть видны таблицы:
   - ✅ `users`
   - ✅ `profiles`
   - ✅ `assessments`
   - ✅ `answers`

### Проверка функций:

1. В SQL Editor выполните:
```sql
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'public' 
AND routine_type = 'FUNCTION';
```

Должны быть видны функции:
- ✅ `get_active_assessment`
- ✅ `complete_assessment`
- ✅ `calculate_checkup_scores`
- ✅ `update_updated_at_column`
- ✅ `handle_new_user`

---

## ⚠️ Если возникли ошибки

### Ошибка "relation already exists":
- Это значит, что таблица уже существует
- Можно пропустить эту миграцию или удалить таблицу и создать заново

### Ошибка "permission denied":
- Проверьте, что вы вошли как администратор
- Или попробуйте выполнить миграции через панель управления Beget

### Другие ошибки:
- Скопируйте текст ошибки
- Сообщите мне, и я помогу исправить

---

## 🎉 Готово!

После успешного применения всех миграций:
- ✅ База данных готова к работе
- ✅ Безопасность настроена
- ✅ Функции для подсчета баллов созданы

Теперь можно тестировать приложение! 🚀

---

## 📝 Чеклист

- [ ] Открыт SQL Editor в Supabase
- [ ] Применена миграция 001 (таблицы)
- [ ] Применена миграция 002 (RLS)
- [ ] Применена миграция 003 (функции)
- [ ] Проверены таблицы в Table Editor
- [ ] Проверены функции через SQL запрос











