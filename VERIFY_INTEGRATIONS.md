# ✅ Проверка установки интеграций

## Шаг 1: Проверка Cron Jobs

Выполните в SQL Editor:

```sql
-- Проверка созданных задач
SELECT 
  jobid,
  jobname,
  schedule,
  active,
  database,
  command
FROM cron.job 
WHERE jobname IN (
  'update-appointment-statuses', 
  'cleanup-old-assessments', 
  'send-appointment-reminders'
)
ORDER BY jobname;
```

**Ожидаемый результат:** 3 задачи должны быть активны (active = true)

---

## Шаг 2: Проверка очередей

```sql
-- Проверка созданных очередей
SELECT queue_name, created_at 
FROM pgmq.list_queues()
ORDER BY queue_name;
```

**Ожидаемый результат:** Должны быть очереди:
- `analytics_queue`
- `email_queue`
- `payment_processing_queue`
- `report_generation_queue`

---

## Шаг 3: Проверка функций

```sql
-- Проверка функций для работы с очередями
SELECT 
  proname as function_name,
  proargnames as parameters
FROM pg_proc 
WHERE proname IN (
  'queue_email_task',
  'queue_report_generation',
  'queue_payment_processing',
  'send_appointment_reminders'
)
ORDER BY proname;
```

**Ожидаемый результат:** 4 функции должны существовать

---

## Шаг 4: Проверка триггеров

```sql
-- Проверка созданных триггеров
SELECT 
  trigger_name,
  event_object_table,
  event_manipulation,
  action_timing
FROM information_schema.triggers 
WHERE trigger_schema = 'public' 
  AND trigger_name IN (
    'on_checkup_completed', 
    'on_new_appointment', 
    'on_payment_status_change'
  )
ORDER BY trigger_name;
```

**Ожидаемый результат:** 3 триггера должны существовать

---

## Шаг 5: Тестовая проверка работы

### Тест 1: Проверка функции отправки email в очередь

```sql
-- Тест добавления задачи в очередь email
SELECT public.queue_email_task(
  'test@example.com',
  'Тестовое письмо',
  'test_template',
  '{"test": true}'::jsonb
);
```

**Ожидаемый результат:** Должен вернуться ID сообщения (bigint)

### Тест 2: Проверка очереди email

```sql
-- Проверка задач в очереди email
SELECT 
  msg_id,
  enqueued_at,
  msg->>'to' as email_to,
  msg->>'subject' as subject
FROM pgmq.email_queue
ORDER BY enqueued_at DESC
LIMIT 5;
```

**Ожидаемый результат:** Должна быть видна тестовая задача

---

## 🎉 Если все проверки прошли успешно

Все интеграции установлены и работают! Теперь:

1. ✅ **Cron jobs** автоматически обновляют статусы каждые 5 минут
2. ✅ **Триггеры** автоматически обрабатывают события (завершение чекапов, новые записи, платежи)
3. ✅ **Очереди** готовы к использованию для фоновых задач

## ⚠️ Что нужно настроить дальше

1. **Обработчики очередей (Workers)** - для выполнения задач из очередей
2. **SMTP настройки** - для отправки email
3. **Мониторинг** - для отслеживания работы системы

См. `INTEGRATIONS_SETUP.md` для подробностей.







