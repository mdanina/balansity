# Локальное тестирование webhook с ngrok

## 🤔 Что такое ngrok и зачем он нужен?

**Проблема:** 
- Ваш API сервер работает локально на `http://localhost:3001`
- ЮKassa не может отправить webhook на `localhost` (это ваш компьютер, недоступен из интернета)
- Нужен публичный URL, который будет перенаправлять запросы на ваш локальный сервер

**Решение: ngrok**
- Создает временный публичный URL (например, `https://abc123.ngrok.io`)
- Все запросы на этот URL автоматически перенаправляются на ваш `localhost:3001`
- ЮKassa может отправлять webhook на этот публичный URL
- Вы получаете webhook локально для тестирования

## 📦 Установка ngrok

### Windows

1. Скачайте с [ngrok.com](https://ngrok.com/download)
2. Распакуйте `ngrok.exe` в любую папку
3. Или установите через Chocolatey:
   ```bash
   choco install ngrok
   ```

### Mac

```bash
brew install ngrok
```

### Linux

```bash
# Скачайте и распакуйте
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar -xzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/
```

## 🚀 Использование

### Шаг 1: Запустите ваш API сервер

```bash
cd queue-worker
npm run dev
```

Сервер должен работать на `http://localhost:3001`

### Шаг 2: Запустите ngrok

Откройте **новый терминал** и выполните:

```bash
ngrok http 3001
```

Вы увидите что-то вроде:

```
Session Status                online
Account                       ваш-email@example.com
Version                       3.x.x
Region                        United States (us)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123-def456.ngrok.io -> http://localhost:3001

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**Важно:** Скопируйте URL из строки `Forwarding` - это ваш публичный URL!

### Шаг 3: Настройте webhook в ЮKassa

1. Зайдите в личный кабинет ЮKassa
2. Перейдите в "Настройки" → "Уведомления"
3. Добавьте URL:
   ```
   https://abc123-def456.ngrok.io/api/webhook/yookassa
   ```
   (замените на ваш URL из ngrok)

4. Сохраните

### Шаг 4: Тестируйте

Теперь когда ЮKassa отправляет webhook, он попадет на ваш локальный сервер!

## 🔍 Мониторинг запросов

ngrok предоставляет веб-интерфейс для просмотра всех запросов:

1. Откройте в браузере: `http://127.0.0.1:4040`
2. Вы увидите все запросы, которые приходят через ngrok
3. Можно просматривать заголовки, тело запроса, ответы

Это очень полезно для отладки!

## ⚠️ Важные моменты

### 1. URL меняется при каждом запуске

Каждый раз когда вы запускаете ngrok, URL будет разный:
- Сегодня: `https://abc123.ngrok.io`
- Завтра: `https://xyz789.ngrok.io`

**Решение:** Нужно обновлять webhook URL в ЮKassa при каждом запуске.

**Альтернатива:** Зарегистрируйтесь на ngrok.com (бесплатно) и получите постоянный домен:
```bash
ngrok config add-authtoken ваш-токен
ngrok http 3001 --domain=ваш-постоянный-домен.ngrok.io
```

### 2. Бесплатный план имеет ограничения

- Ограничение по количеству соединений
- URL меняется при каждом запуске
- Для постоянного домена нужен платный план

### 3. Безопасность

- ngrok URL публичный - любой может отправить запрос
- В продакшне используйте проверку IP адресов или подписи
- Для тестирования это нормально

## 🔄 Альтернативы ngrok

### 1. Cloudflare Tunnel (бесплатно, постоянный домен)

```bash
# Установка
npm install -g cloudflared

# Запуск
cloudflared tunnel --url http://localhost:3001
```

### 2. localtunnel (простой, но менее стабильный)

```bash
npm install -g localtunnel
lt --port 3001
```

### 3. serveo (без установки)

```bash
ssh -R 80:localhost:3001 serveo.net
```

## 📝 Пример полного workflow

```bash
# Терминал 1: Запуск API сервера
cd queue-worker
npm run dev

# Терминал 2: Запуск ngrok
ngrok http 3001

# Скопируйте URL (например: https://abc123.ngrok.io)
# Добавьте в ЮKassa webhook: https://abc123.ngrok.io/api/webhook/yookassa

# Терминал 3: Запуск frontend
npm run dev

# Теперь тестируйте оплату!
```

## 🎯 Рекомендации

### Для разработки:
- Используйте ngrok с бесплатным аккаунтом
- Обновляйте webhook URL при каждом запуске
- Используйте веб-интерфейс ngrok для отладки

### Для тестирования:
- Используйте тестовый сервер с постоянным доменом
- Или ngrok с постоянным доменом (платный план)

### Для продакшна:
- Используйте реальный домен
- Настройте nginx для проксирования
- Настройте SSL сертификат

## ❓ Частые вопросы

**Q: Нужно ли ngrok для продакшна?**  
A: Нет! В продакшне используйте реальный домен с nginx.

**Q: Можно ли тестировать без webhook?**  
A: Да, но статусы платежей не будут обновляться автоматически. Нужно будет проверять вручную через API.

**Q: Безопасно ли использовать ngrok?**  
A: Для локального тестирования - да. В продакшне не используйте.

**Q: Сколько стоит ngrok?**  
A: Бесплатный план достаточен для разработки. Платный нужен только для постоянного домена.

## 🔗 Полезные ссылки

- [ngrok.com](https://ngrok.com/)
- [Документация ngrok](https://ngrok.com/docs)
- [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)







