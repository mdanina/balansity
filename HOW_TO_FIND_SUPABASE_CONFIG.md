# 🔍 Как найти и настроить конфигурацию Supabase на сервере

## 📂 Где искать файлы конфигурации

### Вариант 1: Docker Compose (самый распространенный)

Файл `docker-compose.yml` обычно находится в:
- `/opt/supabase/` (стандартная установка)
- `/home/user/supabase/`
- `/var/www/supabase/`
- Или в папке, где вы устанавливали Supabase

**Как найти:**
```bash
# На сервере выполните:
find / -name "docker-compose.yml" 2>/dev/null | grep -i supabase
# или
find / -name "*.yml" -path "*/supabase/*" 2>/dev/null
```

### Вариант 2: .env файл

Файл `.env` может быть:
- В той же папке, что и `docker-compose.yml`
- В `/opt/supabase/.env`
- В `/home/user/supabase/.env`

**Как найти:**
```bash
find / -name ".env" -path "*/supabase/*" 2>/dev/null
```

### Вариант 3: Конфигурация GoTrue

Если Supabase установлен через отдельные сервисы, конфигурация может быть в:
- `/etc/supabase/`
- `/opt/supabase/config/`
- В папке с приложением

## 🔧 Что нужно изменить

### В docker-compose.yml

Найдите секцию `auth` (или `gotrue`) и добавьте/измените переменные окружения:

```yaml
services:
  auth:
    # ... другие настройки ...
    environment:
      # Добавьте или измените эти строки:
      GOTRUE_MAILER_AUTOCONFIRM: "true"  # true = без подтверждения
      GOTRUE_SITE_URL: "http://localhost:8080"
      GOTRUE_URI_ALLOW_LIST: "http://localhost:8080/email-confirm,http://localhost:8080"
```

### В .env файле

Добавьте или измените строки:

```env
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SITE_URL=http://localhost:8080
GOTRUE_URI_ALLOW_LIST=http://localhost:8080/email-confirm,http://localhost:8080
```

## 📝 Пошаговая инструкция

### Шаг 1: Подключитесь к серверу

Если у вас SSH доступ:
```bash
ssh user@oyuyienekon.beget.app
```

Или используйте панель управления Beget для доступа к файлам.

### Шаг 2: Найдите файлы конфигурации

```bash
# Перейдите в папку Supabase (замените на вашу)
cd /opt/supabase
# или
cd /home/user/supabase

# Посмотрите, какие файлы есть
ls -la
```

### Шаг 3: Отредактируйте конфигурацию

**Если есть docker-compose.yml:**
```bash
nano docker-compose.yml
# или
vi docker-compose.yml
```

**Если есть .env:**
```bash
nano .env
# или
vi .env
```

### Шаг 4: Добавьте переменные

См. примеры выше в разделе "Что нужно изменить"

### Шаг 5: Перезапустите сервисы

```bash
# Если используете docker-compose
docker-compose restart auth
# или
docker-compose down && docker-compose up -d

# Если используете systemd
sudo systemctl restart supabase-auth
```

## 🎯 Быстрый способ через Beget

Если у вас нет SSH доступа, но есть панель Beget:

1. Войдите в панель управления Beget
2. Найдите установленное приложение Supabase
3. Перейдите в "Файловый менеджер" или "Настройки"
4. Найдите файлы `docker-compose.yml` или `.env`
5. Отредактируйте их через веб-интерфейс
6. Перезапустите приложение

## ✅ Проверка

После изменения конфигурации проверьте:

```bash
# Проверьте переменные окружения контейнера
docker-compose exec auth env | grep GOTRUE

# Или если используете docker напрямую
docker exec <container_name> env | grep GOTRUE
```

Должны быть видны ваши переменные:
- `GOTRUE_MAILER_AUTOCONFIRM=true`
- `GOTRUE_SITE_URL=...`
- и т.д.

## 🆘 Если не можете найти файлы

1. **Свяжитесь с поддержкой Beget** - они могут подсказать, где находятся файлы
2. **Проверьте документацию** вашей установки Supabase
3. **Используйте веб-интерфейс** - возможно, настройки можно изменить через админ-панель Supabase

