# 🗄️ Применение миграции 019: Консультации и оплата

## ⚠️ Ошибка
Если вы видите ошибку:
```
ERROR: 42P01: relation "public.payments" does not exist
```

Это означает, что миграция 019 еще не применена. Следуйте инструкциям ниже.

---

## 📍 Шаг 1: Откройте SQL Editor в Supabase

1. В админ-панели Supabase перейдите в **SQL Editor**
   - Обычно в меню слева или по адресу: `https://oyuyienekon.beget.app/project/default/sql`
   
2. Нажмите **"New query"** (Новый запрос)

---

## 📝 Шаг 2: Примените миграцию 019

### Что делает эта миграция:
- ✅ Создает таблицу `appointment_types` (типы консультаций)
- ✅ Создает таблицу `appointments` (записи на консультации)
- ✅ Создает таблицу `packages` (пакеты сессий)
- ✅ Создает таблицу `package_purchases` (покупки пакетов)
- ✅ Создает таблицу `payments` (платежи)
- ✅ Настраивает RLS политики безопасности
- ✅ Создает индексы для оптимизации

### Как применить:

1. Откройте файл `supabase/migrations/019_appointments_and_payments.sql` в вашем проекте
2. **Выделите весь текст** в файле (Ctrl+A)
3. **Скопируйте** (Ctrl+C)
4. Вставьте в SQL Editor в Supabase (Ctrl+V)
5. Нажмите **Run** (Выполнить) или `Ctrl+Enter`
6. Дождитесь сообщения **"Success"** или **"Успешно"**

⚠️ **Важно:** 
- Миграция использует `IF NOT EXISTS`, поэтому безопасно запускать несколько раз
- Если увидите ошибки, скопируйте их текст и сообщите мне!

---

## ✅ Шаг 3: Проверка

### Проверка таблиц:

1. В админ-панели найдите **Table Editor** (Редактор таблиц)
2. Должны быть видны новые таблицы:
   - ✅ `appointment_types`
   - ✅ `appointments`
   - ✅ `packages`
   - ✅ `package_purchases`
   - ✅ `payments`

### Проверка через SQL:

Выполните в SQL Editor:
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('appointment_types', 'appointments', 'packages', 'package_purchases', 'payments')
ORDER BY table_name;
```

Должны вернуться все 5 таблиц.

---

## 📊 Шаг 4: Добавление начальных данных (опционально)

После применения миграции можно добавить тестовые типы консультаций и пакеты:

### Добавить типы консультаций:

```sql
INSERT INTO public.appointment_types (name, duration_minutes, price, description, is_active)
VALUES
  ('Первичная встреча', 30, 0.00, 'Бесплатная первичная консультация с персональным координатором', true),
  ('Сессия для ребенка', 45, 5000.00, 'Сессия детской терапии со специалистом', true),
  ('Сессия для родителя', 45, 5000.00, 'Сессия со специалистом для родителей', true);
```

### Добавить пакеты:

```sql
-- Сначала получите ID типов консультаций
SELECT id, name FROM public.appointment_types;

-- Затем добавьте пакеты (замените appointment_type_id на реальные ID)
INSERT INTO public.packages (name, session_count, appointment_type_id, price, description, is_active)
VALUES
  ('4 сессии для ребенка', 4, 'ЗАМЕНИТЕ_НА_ID_СЕССИЯ_ДЛЯ_РЕБЕНКА', 18000.00, 'Пакет из 4 сессий детской терапии', true),
  ('8 сессий для ребенка', 8, 'ЗАМЕНИТЕ_НА_ID_СЕССИЯ_ДЛЯ_РЕБЕНКА', 35000.00, 'Пакет из 8 сессий детской терапии', true),
  ('12 сессий для ребенка', 12, 'ЗАМЕНИТЕ_НА_ID_СЕССИЯ_ДЛЯ_РЕБЕНКА', 50000.00, 'Пакет из 12 сессий детской терапии', true),
  ('4 сессии для родителя', 4, 'ЗАМЕНИТЕ_НА_ID_СЕССИЯ_ДЛЯ_РОДИТЕЛЯ', 18000.00, 'Пакет из 4 сессий со специалистом для родителей', true),
  ('8 сессий для родителя', 8, 'ЗАМЕНИТЕ_НА_ID_СЕССИЯ_ДЛЯ_РОДИТЕЛЯ', 35000.00, 'Пакет из 8 сессий со специалистом для родителей', true),
  ('12 сессий для родителя', 12, 'ЗАМЕНИТЕ_НА_ID_СЕССИЯ_ДЛЯ_РОДИТЕЛЯ', 50000.00, 'Пакет из 12 сессий со специалистом для родителей', true);
```

---

## ⚠️ Если возникли ошибки

### Ошибка "relation already exists":
- Это значит, что таблица уже существует
- Миграция использует `IF NOT EXISTS`, поэтому это нормально
- Можно продолжить

### Ошибка "permission denied":
- Проверьте, что вы вошли как администратор
- Или попробуйте выполнить миграции через панель управления Beget

### Ошибка "foreign key constraint":
- Убедитесь, что таблицы `users` и `profiles` уже существуют
- Примените сначала миграции 001-018, если еще не применяли

### Другие ошибки:
- Скопируйте текст ошибки
- Сообщите мне, и я помогу исправить

---

## 🎉 Готово!

После успешного применения миграции:
- ✅ Таблицы для консультаций и оплаты созданы
- ✅ Безопасность (RLS) настроена
- ✅ Функционал консультаций готов к использованию

Теперь можно:
1. Открыть приложение
2. Перейти в Dashboard
3. Нажать "Получить консультацию"
4. Протестировать весь flow!

---

## 📝 Чеклист

- [ ] Открыт SQL Editor в Supabase
- [ ] Применена миграция 019 (скопирован и выполнен весь SQL)
- [ ] Проверены таблицы в Table Editor (5 новых таблиц)
- [ ] (Опционально) Добавлены начальные данные (типы консультаций и пакеты)
- [ ] Протестирован функционал в приложении

